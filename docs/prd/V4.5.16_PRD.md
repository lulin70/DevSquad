# DevSquad V4.5.16 PRD — Housekeeping + Digital Truth PATCH

> **日期**: 2026-09-03　**基线**: v4.5.15 (`14b9d3c`)　**类型**: PATCH（无新用户功能、无 Breaking）
> **状态**: 已用户裁决（2026-09-03：修 1-4 硬伤 + 5-22 housekeeping + 23-27 V4.6.0 backlog）

## 1. 立项依据（来自 V4.5.15 项目整理评估）

7 维度扫描结论：
- **代码质量 8.5 / 测试覆盖 7.5 / 文档一致性 5.0 / CI/CD 7.0 / 可观测性 8.0 / 生产稳定性 8.5 / 目录结构 8.0 / 安全 7.5 / 架构 8.0**
- **综合 7.7/10**——文档治理 + 数字诚实是当前主要短板
- 用户裁决：本轮 V4.5.16 收口**全部 4 项硬伤 + 18 项 housekeeping + 5 项 V4.6.0 backlog**

## 2. 修复分层（4 + 18 + 5）

### 2.1 硬伤 P0（critical/warning-blocking，~2h，发布前必完成）

- **H1** `_version.py:2` `__version_info__ = (4, 5, 11)` 与 `__version__ = "4.5.15"` 错位 → 依赖元组比较的代码误判
- **H2** `helm/devsquad/values.yaml:5` `image.tag: "4.0.0"` → 运维误以为最新是 4.0.0
- **H3** `scripts/publish_now.sh` V3.7.0 dead code → 删除
- **H4** `test.yml::lint` 缺 `check_module_activation.py` → nightly anti-ghost 漏检

### 2.2 Housekeeping P1（~4h，本周内）

- **P1.1** README-CN/JP.md: 67/12/725/599 行漂移 + CN 行 6-7 `HostBridge → HostLLMBridge`
- **P1.2** SKILL.md:33/92/139 + CLAUDE.md:95/380 + skill-manifest.yaml:455/452 漂移
- **P1.3** INSTALL.md 全文 V4.5.15 同步（行 1 + 行 75）
- **P1.4** MODULE_REFERENCE.md:1/3/143-145 187+→193+ + V4.5.2-15 补全
- **P1.5** `DevSquad → DevSquad` 全局 sed（≥25 文件 39+ 处）
- **P1.6** SPEC.md:1/39/4-5 标题错位 + 版本表 + 日期
- **P1.7** `docs/release_notes/` 命名空间统一（V4.5.5+ 迁入子目录）
- **P1.8** 补 `docs/prd/V4.5.14_PRD.md` stub + `docs/e2e_evidence/V4.5.14_real_listener/` + `V4.5.15_prometheus_e2e/_pending.md` 占位

### 2.3 自动化与质量加固 P2（~3h）

- **P2.1** `scripts/sync_skill_pack.py`（dry-run + 哈希 + 三层同步）+ 测试
- **P2.2** frontmatter 测试补 2 用例（合法 `|` 块 + 4KB 超长）
- **P2.3** `check_module_activation.py` 补 8-10 个 counter 显式 import
- **P2.4** `cli/cli_visual.py` 幽灵入口核查/删除
- **P2.5** `api/routes/{dispatch,lifecycle,metrics_gates}.py` 18 处裸 except 收窄 + `logger.exception`
- **P2.6** `release-e2e.yml:1` name 改 `Release E2E Gate` + `dispatch_steps_base.py` 核查
- **P2.7** helm/values.yaml dashboard envVars 清理 + CLAUDE.md 第 8 行模块列表脚本化
- **P2.8** 顶层 test_*.py 批量迁移至 tests/unit/
- **P2.9** `verify_prometheus_e2e.py` 测试迁至 tests/e2e/ + @pytest.mark.e2e/slow

### 2.4 V4.6.0 backlog 启动 P3（V4.5.16 阶段先建脚本骨架）

- **P3.1** 数字 SSOT 自动化（`_version.py` 加 `MODULE_COUNT`/`TEST_COUNT` + CI 断言）
- **P3.2** `docs/_archive/` 命名按版本号 + `skills/_version.py` re-export 单一 SSOT
- **P3.3** `PERFORMANCE_BASELINE.json` 重新采集脚本骨架（host/api 真实测量）
- **P3.4** `scripts/collaboration/` 按 domain 分包映射文档（不动代码）
- **P3.5** 顶层 135 个 test_*.py 完整盘点报告（避免一刀切误迁）

## 3. 验收标准

- AC-1: H1 元组 `(4, 5, 15)` 与 `__version__ = "4.5.16"` 一致；H2 helm tag = `4.5.16`；H3 publish_now.sh 移至 `docs/_archive/scripts/`；H4 test.yml lint 加 anti-ghost 后 nightly cron 触发
- AC-2: 三语 README 行 67 = 193+ / 行 12 = 9400+ badge / 行 725 = `2026-09-02 | V4.5.15`（V4.5.16 发布时再刷为 V4.5.16）/ 行 599 = `4.5.15`
- AC-3: SKILL.md/CLAUDE.md/SPEC.md/MODULE_REFERENCE.md/INSTALL.md/skill-manifest.yaml 漂移全部修复
- AC-4: `DevSquad` 0 命中（除 V4.5.5/6 PRD 历史锚点可保留在 `_archive/`）
- AC-5: `scripts/sync_skill_pack.py` 提供 `--dry-run` + 三层同步 + 每层 SHA256 哈希对比
- AC-6: frontmatter 测试 ≥6 个（T14 已有 4 + 新增 2）
- AC-7: check_module_activation 显式覆盖 ≥32/34 个 counter（剩 2 个标注已知风险）
- AC-8: cli/cli_visual.py 删除或保留并加 docstring 说明
- AC-9: api/routes 18 处裸 except 全部改具体类型 + logger.exception
- AC-10: verify_prometheus_e2e 测试放在 tests/e2e/，release-e2e.yml 触发

## 4. 门禁

- ruff 0 / ruff format 0
- check_version_consistency 60+ passed（含 3 层缓存）
- check_module_activation 全覆盖
- 定向测试全绿
- 全量回归 ≥9550 passed / 0 failed（V4.5.15 基线）
- commit + tag v4.5.16 + push origin main --tags

## 5. 风险与回滚

- H1 元组修复可能影响依赖 `version_info` 比较的下游代码（grep 验证后确定无影响）
- API routes 异常类型收窄：保留兜底回退到 500 的 catch-all，最后一道防线仍是 `Exception`
- sync_skill_pack.py 干跑（`--dry-run`）默认行为，不会真写任何文件

## 6. 教训预期（本轮完成后登记）

- L-V4516-001 数字 SSOT 自动化优先于手动同步（避免每次发版都要 grep + sed）
- L-V4516-002 文档与代码漂移需双向同步门禁（CI 应跑 `check_docs_consistency.py`）
- L-V4516-003 幽灵入口删除前必须 grep 整个仓库（含测试）确认零引用