# DevSquad V4.5.20 — 7-Role 共识评估记录

> **评估对象**: [V4.5.20_ocr-learnings_PRD.md](file:///Users/lin/trae_projects/DevSQuad/docs/prd/V4.5.20_ocr-learnings_PRD.md)
> **评估日期**: 2026-09-21
> **基线**: V4.5.19 (commit ae0ea8e, tag v4.5.19)
> **来源**: ① alibaba/open-code-review 架构借鉴评估；② `insight-devsquad-2026-09-21.md` 周榜趋势信号（第二轮纳入）
> **评估执行方式**: 宿主 LLM（TRAE IDE 内）按 DevSquad 文档化的 prompt-orchestration 模型执行 7 角色评审。**未调用真实 LLM backend**（CLI mock 模式不产生真实角色输出），此为项目已披露的执行路径，特此标注。
> **结构**: 第一轮 = 7-Role 评分 + R1–R6 强制修订；第二轮 = 通盘核查 + 用户范围决议；**第三轮 = 对定稿方案（含版本固定 4.5.20）的 7-Role 终评 + R7–R9**；第四轮 = D5 定档取证 + R10；第五轮 = W0 执行证据 + D7 闭环（R11）；**第六轮 = W0 发布（版本 4.5.20 + release notes + 门禁/回归）**；**第七轮 = 发布后缺陷登记与 P0 处置（anyio CVE + flaky 判据）**；**第八轮 = auto 链顺序反转 + 推理预算契约收紧（推翻 report-only）+ dispatcher size gate 拆文件（2026-09-23）**
> **版本说明**: 本文档原名为 `V4.6.2_*`。用户 2026-09-21 明确纠正"版本别乱升级，什么都没做版本倒是已经 4.6.2 了"，**统一固定为 4.5.20**，设计阶段不预分配版本号（见 PRD §7.1）。

---

## 评估流程

采用 V4.0+ 标准 7-Role 并行评估流程：

1. 7 个角色独立审阅 PRD
2. 每个角色按 5 维度（0-10 分）评分
3. 加权求总分
4. 任一角色评分 < 7.0 → 触发共识修订
5. 平均分 ≥ 8.0 → 共识通过

---

## 第一轮：评分汇总

| Role | 权重 | 评分 | 维度细分（架构匹配度 / 设计优雅度 / 测试覆盖 / 文档完整度 / 运维可观测） |
|------|------|------|-------------------------------------------------------------|
| Architect | 1/7 | **8.0** | 8.5 / 8 / 8 / 8 / 7.5 |
| PM | 1/7 | **8.0** | 8 / 8.5 / 8 / 8 / 7.5 |
| Security | 1/7 | **8.5** | 8.5 / 8.5 / 8.5 / 8.5 / 8.5 |
| Tester | 1/7 | **8.0** | 8 / 8 / 8 / 8 / 8 |
| Coder | 1/7 | **7.5** | 7 / 7.5 / 8 / 8 / 7.5 |
| DevOps | 1/7 | **8.0** | 8 / 8 / 8 / 8 / 8 |
| UI | 1/7 | **8.5** | 8.5 / 8.5 / 8.5 / 8.5 / 8.5 |
| **加权平均** | — | **8.1** | — |

**共识结果**: ⚠️ **有条件通过（8.1/10 ≥ 8.0）——附 6 项强制修订**

> 无角色低于 7.0，不触发修订重评；但 Coder（7.5）与 Architect/PM/Tester/DevOps（8.0）提出的异议涉及**事实性错误与范围失控**，须落实修订后方可启动实施。

---

## 第一轮：角色详细意见

### 🏛️ Architect (8.0/10)

**亮点**:
- ✅ 第 5 节"明确不改"清单是本 PRD 最有价值的部分——它把"为借鉴而过度设计"这个最大风险变成了显式边界约束
- ✅ 正确识别 Unified Gather Core（V4.5.9）与 OCR 文件打包**正交而非替代**关系，避免了推翻既有实现的冲动
- ✅ E2 作为 E1 前置的依赖顺序正确（规则解析层是过滤门的基础）

**担忧**:
- ⚠️ **W0（PATCH 修复）与 W1–W3（MINOR 新能力）混装同一 PRD**。两者性质、风险、评审门槛都不同，混装会导致 P0 修复被 W1 的设计讨论阻塞
- ⚠️ E1 扩展 `review` 输入契约是**架构级接口变更**，影响面远超 review 本身：`dispatcher` 的 consensus 模式、MCP tool `dispatch`、REST API 均可能间接受影响。PRD 未评估影响面
- ⚠️ F3 定为"要改"略显勉强——它是能力缺口而非缺陷。分类应更严格

**建议**:
- **R4（强制）**：W0 物理拆分为独立 PRD + 独立分支（`v4.5.20-patch`），不被 W1 阻塞
- review 输入契约采用**纯加法式**扩展：新增字段，不改既有字段语义
- F3 从"要改"降为"可进化"，或明确标注为"分类边界情形"

### 📋 PM (8.0/10)

**亮点**:
- ✅ 要改 / 可进化的二分清晰，F1 提供完整证据表（10 行 grep 结果 + 文件行号），不是空口断言
- ✅ 波次划分对应可独立发布的单元，符合"每步可交付"
- ✅ W0-2 的负向测试要求（人为篡改验证门禁变红）体现了正确的门禁观

**担忧**:
- ⚠️ **E6（外部基准）投入产出未论证就进了 PRD**。它自认"投入最大""可能被质疑自评""建议后置"，却仍占 P3 波次名额——这本身就是 PRD 第 5 节想禁止的过度设计
- ⚠️ **优先级与价值不匹配**：E5 被标 P2，但它"几乎不需新代码"且直接改善产品定位，是性价比最高项；E6 标 P3 却投入最大。排序应按"价值/成本比"而非字母序
- ⚠️ 第 9 节 4 个待决策项全部悬空，无一个是"已决策"，PRD 的决策完成度不足

**建议**:
- **R3（强制）**：E6 从本 PRD 降级为 Backlog 提案（仅保留问题描述与风险提示，不承诺波次与版本）；E5 升为 P1，与 W1 并行推进

### 🔒 Security (8.5/10)

**亮点**:
- ✅ E1 明确要求 `secret_exclude` 无条件优先且不可被 `include` 覆盖，语义正确
- ✅ 第 5 节确认现有安全门已 fail-closed（`InputValidator.validate_task()` 返回 `valid=False`；`rbac_fail_closed=True` 注释"禁止 fail-open"），不盲目改
- ✅ 未照搬 OCR 的做法，而是在对照表中单列"已达标，不改"

**担忧**:
- ⚠️ **E2 新增的项目级 / 用户级规则文件是全新攻击面**。`~/.devsquad/rule.json` 与 `<repo>/.devsquad/rule.json` 可被本地篡改；若优先级链允许用户层覆盖安全相关规则（如把敏感路径加入 `include` 以强制审查），将打开绕过路径。PRD 的 E2 只描述了优先级链，**未写死安全豁免规则**
- ⚠️ E1 的 `--preview` 会打印"将审哪些文件、排除了什么"——预览输出可能泄露敏感路径名（如 `.env.production`）到日志/CI 输出

**建议**:
- **R2（强制）**：E2 优先级链必须显式写死"**安全类规则永远在全部用户配置之前求值，且不可被任何层覆盖**"，并附负向测试（用户级 `rule.json` 尝试 `include` 敏感路径必须失败）
- `--preview` 输出敏感路径时需脱敏或仅输出计数

### 🧪 Tester (8.0/10)

**亮点**:
- ✅ 每个 Wave 都配了校验方法，不是"做完再说"
- ✅ W0-2 要求门禁可被负向测试证伪——这是门禁有效性的唯一判据
- ✅ W1-5 保留向后兼容测试，覆盖了接口扩展的主要风险

**担忧**:
- ⚠️ **W1-4 的断言方式有陷阱**："断言 preview 不触发任何 LLM 调用（mock 计数为 0）"——若只用 backend 层计数，会漏掉内部隐式路径。项目已有明确教训：**monkeypatch 必须打到底层**，patch 上层则其内部埋点不被测到
- ⚠️ **E4 的"锚点一致率提升"不可证**：PRD 未定义基线数值。没有基线就无法判定"提升"，只能事后自证
- ⚠️ E3 的"注入一个必失败的 worker"未说明如何在 `asyncio.gather(return_exceptions=True)` 语义下构造可复现失败

**建议**:
- **R5（强制）**：W1-4 断言改为 patch 底层 HTTP client；E4 上线前**必须先测出当前基线数值**并记录，否则该项不可验收

### 💻 Coder (7.5/10)

**亮点**:
- ✅ E2 的"第一个匹配生效、不 merge"实现简单、无状态、易测，方向正确
- ✅ review 输入契约保留单字符串兼容路径，降低了破坏风险

**担忧**:
- ❌ **E1 的"可复用资产"判断有事实错误**。`scripts/qa/deterministic_rule_engine.py` 是 **1576 行**且已深度耦合：46 条 UI/UX 专用规则、7 设计支柱、`TasteDials` 阈值调节、`PILLAR_TO_CATEGORY` 向后兼容映射。"抽象成通用规则解析层"实为一次**大规模重构**，PRD 把风险严重低估
- ⚠️ 项目 Backlog 中已有 **16 个 >800 LOC 历史超大文件待拆**，此文件本身就在名单上。往其中追加职责与既有技术债方向相反
- ⚠️ E1 与 E2 被拆成两项，但实际上"具名过滤门 + 规则匹配"是同一件事的两面，分列容易造成重复实现

**建议**:
- **R1（强制）**：**不要动 `deterministic_rule_engine.py`**。新建独立的轻量规则解析模块（仅 glob 匹配 + 优先级链，约 100 行），`deterministic_rule_engine` 后续**可选**接入，不作为本轮目标。避免为复用而复用

### ⚙️ DevOps (8.0/10)

**亮点**:
- ✅ W0 可独立发布（4.5.20），降低了门禁变更的发布风险
- ✅ 新增门禁项方向正确，且要求先在本地跑通
- ✅ 风险表已识别"新增门禁项可能误报"

**担忧**:
- ⚠️ **W2-2 "覆盖不全 → 非零退出"会改变 `devsquad review` 的退出码语义**，可能破坏既有 CI 消费者。PRD 的 D4 识别了该问题但未解决，却已把 W2-2 写入实施计划
- ⚠️ 门禁扩围会改变 `check_version_consistency.py` 的**检查项数量**（64 → 64+N）。若有 CI 步骤或文档断言了项数，会连带失败
- ⚠️ W3-1 把宿主 LLM 提为默认路径，会改变 IDE 外调用的默认行为，与 SKILL.md 已披露的降级语义存在冲突风险

**建议**:
- **R6（强制）**：D4 必须先解决——grep 全仓确认有无脚本/文档依赖 `review` 退出码与门禁项数；未确认前 W2-2 不得实施

### 🎨 UI (8.5/10)

**亮点**:
- ✅ `rules check` 与 `--preview` 的输出格式在 PRD 中已被描述为结构化（层 + 模式 + 规则正文），具备可读性设计意识
- ✅ E5 的定位调整会让 SKILL.md 的价值主张更清晰，属于信息架构改善

**担忧**:
- ⚠️ PRD 未规定 `rules check` / `--preview` 的输出视觉规范，存在与既有 `ReportFormatter` 风格不一致的风险
- ⚠️ `--preview` 在大型仓库可能输出数百行文件列表，缺少分页/汇总策略

**建议**:
- 两个新命令的输出复用 `ReportFormatter` 既有视觉规范
- `--preview` 默认输出汇总计数，`--verbose` 才展开明细

---

## 第一轮：共识结论

| 维度 | 结论 |
|------|------|
| **评估质量** | ✅ 要改 / 可进化二分有证据支撑，F1 为 ~~10 行 grep 实证~~ **【第三轮更正】实际为 11 处（8 处 `version` + 3 处 `version_source`），第一轮的"10 行"计数与行数均不准确** |
| **事实准确性** | ⚠️ E1 "可复用 deterministic_rule_engine" 判断错误，须修正 |
| **范围控制** | ⚠️ E6 范围失控，须降级 |
| **安全设计** | ⚠️ E2 新配置层未写死安全豁免，须补 |
| **可验收性** | ⚠️ E4 无基线、W1-4 断言方式有陷阱，须补 |
| **发布耦合** | ⚠️ W0/W1 混装、D4 未解决，须拆分与前置 |
| **不需要改的判定** | ✅ 安全门已达标等 6 项"明确不改"判断准确，有效防止了过度设计 |

**最终决议**: ⚠️ **有条件通过 — 落实 R1–R6 六项强制修订后启动实施**

---

## 强制修订清单（R1–R6）

| 编号 | 提出方 | 修订内容 | 验收方式 |
|---|---|---|---|
| **R1** | Coder（Architect 附议） | E1 删除"复用 `deterministic_rule_engine.py`"表述，改为**新建独立轻量规则解析模块**（glob + 优先级链，约 100 行）；该文件不动 | PRD 文本修正 + 不产生对该文件的改动 |
| **R2** | Security | E2 优先级链写死"安全类规则永远在全部用户配置之前，不可被任何层覆盖" + 负向测试 | 负向测试：用户级 rule.json 尝试 include 敏感路径必须失败 |
| **R3** | PM | E6 降级为 Backlog 提案（不承诺波次/版本）；E5 升为 P1，与 W1 并行 | PRD 第 6 节波次表修正 |
| **R4** | Architect | W0 拆为独立 PRD + 独立分支，不被 W1 阻塞 | 新增 `V4.5.20` PRD（或本 PRD 内显式声明 W0 独立交付） |
| **R5** | Tester | W1-4 断言 patch 底层 HTTP client；E4 必须先测当前基线数值 | 测试代码审查 + 基线数值落档 |
| **R6** | DevOps | D4 前置：grep 确认 `review` 退出码与门禁项数的既有依赖 | grep 结果落档，未确认前 W2-2 不实施 |

---

## 修订落实记录（R1–R6 回写 PRD）

| 编号 | 落实状态 | 落地位置（`V4.5.20_ocr-learnings_PRD.md`） | 实际改动 |
|---|---|---|---|
| R1 | ✅ 已落实 | §4 E1「~~可复用资产~~」段 | 原判断作废并保留删除线以示留痕；改为「新建独立轻量规则解析模块（约 100 行），`deterministic_rule_engine.py` 本轮不动」 |
| R2 | ✅ 已落实 | §4 E2「四个必须保留的设计细节」第 4 条 | 由「三个」改「四个」；新增安全豁免条目，明确"永远在 1–3 层之前求值、不可被任何层覆盖、必须写死在代码而非文档" |
| R3 | ✅ 已落实 | §4 E5 / E6、§6 W1 / W3、§7、§9 D1 / D3 | E5 标题改【P1，R3 修订】并补"为何升为 P1"；E6 改【Backlog 提案】+ 显式声明退出波次；W3 波次取消，E5 并入 W1（新增 W1-6 行）；§7 表改为 W0 + W1–W2；D3 标为已决、D1 标为前半已决 |
| R4 | ⚠️ 已落实（**第三轮二次修订**） | §6 W0 段 + §7.2 发布单元列 + 文首 Branch | **一次修订**：W0 标注为独立交付单元 + 计划新建独立 PRD `V4.5.20_version-ssot_PRD.md`。**二次修订（第三轮）**：取消单独建 PRD（本 PRD 已固定 4.5.20，同版本号双 PRD 会制造文档碎片），改为**独立分支 `v4.5.20-patch` + 可独立 tag**，W0 定义以本 PRD §3 F1/F2 + W0 表为准 |
| R5 | ✅ 已落实 | §6 W1-4 与 W2-3 行的校验方法列 | W1-4 改为「patch 底层 HTTP client（`httpx`/`requests` 发送方法），断言调用次数为 0」，并注明不可只 patch backend 层；W2-3 补「必须先测并落档当前锚点一致率基线数值，否则不可验收」 |
| R6 | ✅ 已落实 | §6 W2-2 行 + §9 D4 | W2-2 标注「D4 为前置条件，未确认前不实施」；D4 展开为前置条件说明 + grep 确认方法（`cli.py` / `.github/workflows/` / `scripts/` 中 `returncode` / `$?` / `exit_code`） |
| 附 | ✅ 已落实 | §3 F3 段首 | 按 Architect 建议标注「分类边界情形，非阻塞」，并说明维持"要改"分类的判据；同时说明该分歧不影响交付内容 |

**说明**：以上均为**文档修订**，未产生任何代码改动（符合"文档先行：讨论 → 改文档 → 确认 → 改代码"的项目既定顺序）。W0 实施前无需再建 PRD；其验收基线以 PRD §3 F1 修正后的 **11 行证据表**为准。

---

## 第二轮：通盘核查 + 范围决议（2026-09-21）

### 触发

用户提问：**"规则四层优先级链 + `rules check`、六重门过滤（每个排除都有名字）、委托模式为主、工程化细节可直接抄进 SKILL.md、定位与反思拆成独立模块 —— 这些特性都反映在我们的 P0-P2 了吗？先通盘考虑，方案先行"**
并附带 `insight-devsquad-2026-09-21.md`（GitHub 周榜趋势洞察）。

### 覆盖度核对结论（回答用户提问）

| # | 特性 | 原 PRD 落点 | 覆盖判定 |
|---|---|---|---|
| 1 | 四层优先级链 | E2 → W1-1 | ✅ 完整 |
| 2 | `rules check` | E2 → W1-2 | ✅ 完整 |
| 3 | 六重门过滤（具名排除） | E1 → W1-3 | ⚠️ 覆盖，但前提表述有误（→ N3） |
| 4 | 委托模式为主 | E5 → W1-6 | ⚠️ 仅 1 行，缺工程细节（→ 已补强 5 项） |
| 5 | **工程化细节抄进 SKILL.md** | **无** | ❌ **完全未覆盖**（→ 新增 E7） |
| 6 | 定位与反思拆成独立模块 | E4 → W2-3 | ❌ **证据错误**（→ N2） |

### 通盘核查的新发现

| 编号 | 发现 | 证据强度 | 处置 |
|---|---|---|---|
| **N1 → F4** | `apply_file_bundling` 全仓**无调用方**；V4.5.0 声称的"review 模式确定性打包"从未生效；名为 e2e 的测试实为直接 import 模块 | 强（grep 全仓仅 2 处命中：定义 + release notes） | 新增 **F4（P0）**，进 W0 |
| **N1 附带** | 反幽灵门禁**自调用**模块以触发计数器 → `counter > 0` 断言可能**自证** | 中（现象确认；影响面待核实） | 新增 **D7（待核实）**，风险等级"高（若确认）" |
| **N2** | `AnchorChecker` 是**目标漂移检测**，不是 OCR 的代码行定位 —— E4 原前提为误读 | 强（模块自述 "Goal Alignment Engine"） | E4 定义改写 |
| **N3** | E1 原写"review 没有多文件"仅对 **skill 层**成立；dispatcher 层已有 review mode + 打包设计 | 强（coordinator.py:201） | E1 范围改为"接通 + 补门" |

### 用户 4 项决议

| # | 决策点 | 用户选择 | 落地位置 |
|---|---|---|---|
| 1 | N1/F4 孤儿特性如何处理 | **升 P0 进 W0 修复**（接通真实调用点） | PRD §3 F4、§6 W0-3/W0-4、D5 |
| 2 | E7 契约文档时点 | **W1-0 文档先行**（先写契约作为 W1 规格） | PRD §4 E7、§6 W1-0 |
| 3 | N2/E4 修正后如何处置 | **保留 W2，改写定义**（positioning 新建 + reflection 复用） | PRD §4 E4、§6 W2-3、D2 |
| 4 | insight 其余 3 个信号是否纳入 | **纳入本轮 P1/P2** | PRD §4 E8/E9/E10、§6 W1-7 与 W3 |

### 影响评估（触发哪些角色关注）

| 角色 | 关注点 | 已采取的缓解 |
|---|---|---|
| **PM** | ⚠️ E8/E9/E10 纳入与本角色原有的"过度设计"警告**存在张力** | 已在 §4 为三者分别设边界：E8 限"可解释"、E9/E10 必须过立项论证门槛（W3-0）；§8 风险表显式记录该张力 |
| **Tester** | F4 需"经过管线"的集成测试（不得再直接 import）；E7 需 grep 反查门禁化；E8/E9/E10 无既有基线 | W1-0 / W1-7 / W3-2 校验列均已写明；F4 测试纪律要求写入 §3 F4 |
| **Architect** | F4 接线位置；E9 平台化三层的架构定位；E4 positioning 与既有 OutputValidator 的关系 | D5 保留"接线位置"为执行层决定；E9 前置立项论证 |
| **DevOps** | F4 接通后产生**可观测行为变化**，需 release notes 标注；D7 门禁盲区影响面可能很大 | §7 已写明"必须显式标注，不得静默变更"；D7 列为待核实高风险 |
| **Security** | E7 契约文档必须与 R2 的安全豁免**保持一致**（否则文档漂移会重新打开绕过路径） | E7 交付内容表已含"门语义 + secret_exclude 不可被覆盖" |
| **Coder** | F4 接线实现；E8 不得引入推荐模型/学习型排序 | §4 E8 已写边界约束 |
| **UI** | 本轮新增项无 UI 变更 | — |

### 本轮评审方式的诚实声明

- 第二轮以**用户决议**驱动，**未执行完整的 7-Role 重新评分**（范围扩大的主要判断由用户作出）
- 上表"影响评估"是我按各角色关注点**代拟的关注项**，非各角色独立出具的意见 —— **不得当作 7-Role 评分使用**
- **需要触发 7-Role 复核的条件**：① D7 核实确认门禁自证盲区（可能动摇"反幽灵"承诺）；② W3-0 立项论证结论为"建议实施"时（E9/E10 属战略级）；③ F4 若选择"废弃"路径而非"接通"

---

## 第三轮：定稿方案的 7-Role 终评（2026-09-21）

### 触发

用户指令：**"对现在的方案进行各角色评估，还有，版本别乱升级，什么都没做版本倒是已经 4.6.2 了？先固定在 4.5.20，我们先把方案整理清楚"**

即两点：① 对**当前定稿方案**（含 F4 + E7–E10 + 版本固定 4.5.20）做真正的 7 角色评分；② 纠正版本纪律。

### 评估前的方案基线（本轮评分对象）

| 波次 | 内容 | 版本/分支 |
|---|---|---|
| **W0**（P0，立即） | F1（11 处版本漂移修复）+ F2（门禁扩围）+ F4（孤儿特性接通）+ W0-4（D7 附带核实） | PATCH 4.5.20；分支 `v4.5.20-patch`；可独立 tag |
| **W1** | W1-0 E7 契约先行 → W1-1/W1-2 E2 → W1-3/4/5 E1 → W1-6 E5 → W1-7 E8 | 分支 `v4.5.20-ocr-learnings`；版本**不预分配** |
| **W2** | W2-1/W2-2 E3（W2-2 被 D4 阻塞）+ W2-3 E4 | 同上 |
| **W3** | W3-0 立项论证门槛 → E9 + E10（**条件波次**） | 论证通过才成立 |
| Backlog | E6 外部基准（不承诺版本与时间点） | — |

### 本轮新增核查（评分前取证）

**发现：F1 证据表存在事实错误（已修正）**

| 项 | 初版 PRD | 本轮实测（`grep -n '^version:' ` + `grep -n 'version_source' skills/*/skill-manifest.yaml`） |
|---|---|---|
| 漂移处数 | 标题写"8 处"，表列 10 行 | **11 处** = `version` 8 处 + `version_source` 3 处 |
| 遗漏行 | 表中**缺** `skills/prototype/skill-manifest.yaml:2`（`version: "4.2.1"`） | 已补入 |

**性质**：这是**评审流程自身的缺陷** —— 第一轮 7-Role 评审（含 Tester）**未发现纳入 PRD 的证据表与真实 grep 输出不符**。项目既有纪律"评审数据必须包含实际命令输出，防止虚假自评"在**PRD 写作环节**未被落实。已在 PRD §3 F1 添加修正标注，并作为 R7 提出。

### 评分汇总（第三轮）

| Role | 权重 | 评分 | 维度细分（架构匹配度 / 设计优雅度 / 测试覆盖 / 文档完整度 / 运维可观测） | 较第一轮 |
|------|------|------|-------------------------------------------------------------|---------|
| Architect | 1/7 | **8.0** | 8.5 / 8.0 / 7.5 / 8.5 / 7.5 | 持平 |
| PM | 1/7 | **7.6** | 7.5 / 7.5 / 7.5 / 8.5 / 7.0 | ▼ 0.4 |
| Security | 1/7 | **8.5** | 8.5 / 8.5 / 8.5 / 8.5 / 8.5 | 持平 |
| Tester | 1/7 | **7.8** | 8.0 / 7.5 / 7.5 / 8.0 / 8.0 | ▼ 0.2 |
| Coder | 1/7 | **8.0** | 8.0 / 8.0 / 8.0 / 8.5 / 7.5 | ▲ 0.5 |
| DevOps | 1/7 | **8.1** | 8.0 / 8.0 / 8.0 / 8.5 / 8.0 | ▲ 0.1 |
| UI | 1/7 | **8.3** | 8.5 / 8.5 / 8.0 / 8.5 / 8.0 | ▼ 0.2 |
| **加权平均** | — | **8.0** | — | ▼ 0.1 |

**共识结果**: ⚠️ **有条件通过（8.0/10，恰好达到 8.0 阈值）——附 R7–R9**

> **关键信号（不是分数，是趋势）**：分数从 8.1 → 8.0，**方案在"变好"与"变大"之间净持平**。R1–R6 全部落实、版本纪律与 F1 证据均被修正（在变好）；但 E8/E9/E10 纳入放大了范围、两个真正阻塞项（**D5 接线位置**、**D7 门禁自证核实**）在 P0 波次启动前仍悬空（在变大）。
> **结论：方案已经"整理清楚"到可以开工的程度；当前瓶颈是执行，不是继续规划。**

---

### 角色详细意见（第三轮）

#### 🏛️ Architect (8.0/10)

**亮点**:
- ✅ **F4 的发现证明了"通盘核查"这一步的真实价值** —— 全仓调用链检索抓出了 6 处对外文档的虚假宣称，这是纯静态"读代码取证"抓不到的
- ✅ **R4 二次修订是对的选择**：本 PRD 已固定 4.5.20，再建一份 `V4.5.20_version-ssot_PRD.md` 会造成"同版本号双 PRD"，比"W0 被 W1 阻塞"更糟。用"独立分支 + 独立 tag"解决物理隔离是更简的方案
- ✅ W1 内部依赖顺序（E7 契约 → E2 解析层 → E1 门）无环且符合"文档先行"

**担忧**:
- ⚠️ **D5（F4 接线位置）在 W0 启动前仍未定** —— F4 是 P0/立即，但"接到 `dispatch_steps` 哪一步、是否复用 `>5 文件` 硬守卫"没定就等于带着设计空白开工，极易边做边改
- ⚠️ **E9 是架构级扩展（运行时/插件/上下文三层）**，与 W0 的 PATCH 修复同处一份 PRD，性质跨度大。虽带 W3-0 门槛，但"出现在同一份 PRD"本身会持续吸引注意力
- ⚠️ F1 证据错误说明 PRD 的"只读代码取证"缺**复核环节**

**建议**:
- **R8（强制）**：W0 启动前必须先关闭 D5（定接线位置）；不得以"执行层决定"为由带空白开工
- E9 在 W3-0 论证产出前，PRD 内应显式标注"**尚未成立**"，与 E6 的 Backlog 待遇在**表述上**对齐

#### 📋 PM (7.6/10 ▼)

**亮点**:
- ✅ **版本纪律修正（4.6.2 → 4.5.20）是本轮最有价值的改动** —— 它纠正的不是笔误，而是"未交付先占号"的立项习惯
- ✅ R4 二次修订减少了文档碎片，是成本意识的体现
- ✅ F1 证据已按真实 grep 输出修正为 11 行，验收基线可复核

**担忧**:
- ⚠️ **范围仍在扩张，而我的第一轮"过度设计"警告未被消解，只是被"边界约束 + 门槛"包裹**。E6 已退为 Backlog（好），但 E8/E9/E10 又进来了 —— 净范围**没有减少**
- ⚠️ **P2 的语义含糊**：E9/E10 标 P2 却"须过立项论证"，而 E3/E4 标 P2 是"承诺实施"。同一个 P2 承载两种含义，会误导排期
- ⚠️ **决策完成度**：D1 已决、D2/D3/D5 已决，但 **D4/D6/D7 三项仍未闭合**，且 D4 阻塞 W2-2、D7 影响 W0 的结论 —— "待决策项"从第一轮的 4 个变为 3 个，改善有限
- ⚠️ **最大隐忧：规划轮次在增加，交付轮次为零**。方案从 F1–F3 长到 F4 + E1–E10，**却没有一行代码交付**

**建议**:
- **R9（强制）**：明确 E9/E10 的 P2 含义为"**论证优先级 P2**，不构成交付承诺"；W3 标注为**条件波次**
- 把"结束规划、启动 W0"本身作为本轮结论的一部分（见下方共识结论）

#### 🔒 Security (8.5/10)

**亮点**:
- ✅ R2 已落实：E2 的安全豁免被写死为"永远在 1–3 层之前求值、不可被任何层覆盖、**必须写死在代码而非文档**" —— 这是本轮安全侧最关键的一条，且措辞正确地把约束落在代码层
- ✅ E7 契约表已包含 `secret_exclude` 无条件优先，**契约与实现口径一致**，避免文档漂移重新打开绕过路径
- ✅ 第 5 节"明确不改"对既有 fail-closed 的判定准确

**担忧**:
- ⚠️ **E7 与 E2/W1-3 之间存在"文档先写、代码后实现"的时间窗**：SKILL.md 在 W1-0 就声明"`secret_exclude` 不可被覆盖"，而实现要到 W1-3 才落地。**该窗口期内文档承诺强于代码能力** —— 若在窗口内发版，即构成新的"文档有 ≠ 功能可用"（与 F4 同类）
- ⚠️ `--preview` 输出敏感路径的脱敏要求仍只写在评审意见里，**未写进 PRD 的 E1 交付内容**

**建议**:
- **R7 附议**：E7 契约中凡"尚未实现"的条款，必须在 SKILL.md 内标注**生效版本**（或"随 W1-3 生效"），不得让文档承诺超前于代码
- 把 E1 的 `--preview` 脱敏要求（默认只输出计数、`--verbose` 才展开）**从评审意见提升为 PRD 交付内容**

#### 🧪 Tester (7.8/10 ▼)

**亮点**:
- ✅ R5 已落实：W1-4 改为 patch 底层 HTTP client、W2-3 要求先落基线数值 —— 两条都是可证伪的写法
- ✅ §6 每个步骤都有校验方法列，无"做完再说"
- ✅ F4 的测试纪律增量（测试名必须与执行路径一致）已写入 PRD §3

**担忧**:
- ❌ **本轮实测发现 F1 证据表的计数与行数与真实 grep 不符**（见"本轮新增核查"）。**这是评审未能验证纳入 PRD 的证据** —— 第一轮我没有发现。若该错误未被本轮抓出，W0-1 的验收基线就是错的
- ⚠️ **E8/E9/E10 全部无既有测试基线**，而 W3-2 只写"新角色须有完整测试基线" —— **没有说明基线从哪来、由谁测**
- ⚠️ **W2-2 的校验方法仍存在密度不足**："断言部分失败时退出码 ≠ 0"未区分"角色失败"与"基础设施失败"（后者本就该非零），单靠一个断言无法区分是 E3 生效还是偶然非零
- ⚠️ D7（门禁自证核实）被要求"抽查 ≥3 个模块"，但**未定义"确认自证"的判据**（计数器在生产管线调用前的值应为 0？还是应为不存在？）

**建议**:
- **R7（强制）**：写入 PRD 的任何 grep/统计证据，**必须附原始命令与输出**（同项目"评审数据必须含真实命令输出"纪律）；证据表须标注取证时间
- W2-2 补一条**对照组断言**：基础设施失败路径 → 非零；仅部分角色失败 → 非零（且报告含失败角色名）；全成功 → 零。三者同时成立才算 E3 生效
- D7 的"自证"判据先定义再核实：**先在门禁运行前后分别读计数器**，若"门禁自身调用"即满足断言，则判为自证

#### 💻 Coder (8.0/10 ▲ 0.5)

**亮点**:
- ✅ **R1 落实后 E1 的实现边界清晰了**：新建约 100 行轻量解析模块，`deterministic_rule_engine.py`（1576 行、46 条 UI/UX 规则）本轮不动 —— 这是把一次"大规模重构"降回"可交付的小件"，是本轮最大的实现风险削减
- ✅ R4 二次修订减少了一份 PRD 的创建与双向同步成本
- ✅ E2 的"第一个匹配生效、不 merge"仍是最易实现最易测的设计

**担忧**:
- ⚠️ **E7 契约文档（W1-0）与 E2/E1 实现（W1-1~W1-3）存在"写死契约"的耦合**：契约里若出现形态未定的字段名/退出码，实现时必然要回头改契约（触发二次漂移）。**PRD 未规定"契约变更如何回写"**
- ⚠️ E8 只写"不新增推荐模型/学习型排序"，但**未规定排序依据的数据结构**（枚举结果长什么样），实现时自由度仍偏大
- ⚠️ D5 未定（同 Architect）—— F4 的接线点直接决定 W0 的代码形状

**建议**:
- 补"**契约-实现双向同步**"约束：实现若必须偏离 W1-0 契约，须**先改契约再改代码**（沿用项目"讨论 → 改文档 → 确认 → 改代码"顺序），并在 PRD 记录变更
- E8 在 W1-7 前先定**排序依据的输出结构**（哪怕只是字段清单），避免实现期自由发挥

#### ⚙️ DevOps (8.1/10 ▲ 0.1)

**亮点**:
- ✅ R6 落实后 W2-2 被 D4 正确阻塞 —— 退出码是外部契约，先确认消费方再改，方向正确
- ✅ §7 明确"F4 接通会产生可观测行为变化，**必须在 release notes 显式标注**，不得静默变更" —— 这是发布纪律的正确写法
- ✅ W0 可独立 tag，不被 W1 阻塞，降低了门禁变更的发布风险

**担忧**:
- ⚠️ **W0 的"独立 tag"与 §7.1"版本不预分配"存在一处未明说的边界**：W0 确定是 PATCH → 4.5.20；但 W1–W3 完成后版本待判。若 W1 在 W0 之后不久交付，会出现 `4.5.20` 之后直接跳到"发布前才定的号"，**中间状态无版本可引用**（CI/制品/文档引用会悬空）。PRD 未说明 W0 与 W1 之间的版本引用方式
- ⚠️ 门禁扩围（64 → 64+N）到本轮为止**仍只要求"grep 确认无硬编码依赖"**，未要求把该数字本身**移出任何硬编码位置**
- ⚠️ D7 若确认为自证盲区，**修复它属于门禁基础设施变更**，影响面可能触及全部模块的激活判定 —— 但 PRD 未给 D7 修复留波次

**建议**:
- 明确"**W0 发布 4.5.20 后，W1–W3 的版本号在 W1 发布前定**；期间以分支名 `v4.5.20-ocr-learnings` 引用，不使用版本号引用未交付内容"
- 门禁项数改为**动态读取**（避免 64 这个数字被任何地方硬编码）
- D7 若确认，**在 W0 内闭环**（不得留到 W1），因为"反幽灵"是发布可信度的前提

#### 🎨 UI (8.3/10 ▼ 0.2)

**亮点**:
- ✅ `rules check` / `--preview` 的输出结构（层 + 模式 + 规则正文）在 PRD 中已描述，具备可读性设计意识
- ✅ E7 把契约写进 SKILL.md 会实质改善用户对"工具能做什么/不能做什么"的理解，是信息架构改善
- ✅ E5 的定位调整让"零 API Key 委托"这一价值主张更清晰

**担忧**:
- ⚠️ 与第一轮同：`rules check` / `--preview` 的**视觉规范仍未写入 PRD**，只有评审意见提到"复用 ReportFormatter"
- ⚠️ `--preview` 在大型仓库的输出量仍无分页/汇总策略（同第一轮）
- ⚠️ E7 契约写入 SKILL.md 后，**SKILL.md 会显著变长**；信息架构上需要明确"契约内容是否拆到 `docs/reference/`"（PRD §E7 提到写入 `docs/reference/`，但未说 SKILL.md 保留多少）

**建议**:
- E7 明确**分层**：SKILL.md 保留"用户必须知道的承诺"（零 Key / 非零退出 / preview 零 token），细节语义（8 个门的完整含义表）拆到 `docs/reference/`
- `--preview` 默认汇总计数、`--verbose` 展开（**提升为 PRD 交付内容**，与 Security 建议一致）

---

### 共识结论（第三轮）

| 维度 | 结论 |
|------|------|
| **证据可靠性** | ⚠️ F1 证据表被本轮实测证伪（8 处/10 行 → 11 处/11 行），**已修正**；暴露"证据未复核"的流程缺陷 → R7 |
| **版本纪律** | ✅ 已纠正（4.6.2 → 4.5.20，不预分配）；W0/W1 之间的引用方式待明确 → DevOps 建议 |
| **范围控制** | ⚠️ E6 已退 Backlog，但 E8/E9/E10 纳入使净范围**未减少**；E9/E10 的 P2 语义需澄清 → R9 |
| **安全设计** | ✅ R2 已落实且措辞正确；新增风险：E7 契约在 W1-0 先于实现（W1-3）声明 → R7 附议 |
| **可验收性** | ⚠️ W2-2 断言密度不足（无法区分 E3 生效 vs 偶然非零）；D7 判据未定义 |
| **前置项闭合** | ⚠️ D5（阻塞 W0）、D7（应为 W0 内闭环）、D4（阻塞 W2-2）**三项未闭合**；W0 是 P0/立即 → R8 |
| **不需要改的判定** | ✅ 第 5 节 6 项"明确不改"仍然准确，持续抑制过度设计 |

**最终决议**: ⚠️ **有条件通过（8.0/10）—— 落实 R7–R9 后启动 W0；本轮结束"方案整理"阶段**

**本轮的实质判断（最重要的一条）**：
> 方案已经**整理清楚到可以开工**的程度。继续在 PRD 上做加法（新增借鉴项）的边际收益已趋近于零，而风险（范围膨胀、前置项悬空、零交付）在上升。
> **建议：不再向本 PRD 增加任何新的借鉴项；先执行 W0（P0，独立分支 `v4.5.20-patch`），用一次真实交付来验证方案质量。**

---

### 强制修订清单（R7–R9，第三轮）

| 编号 | 提出方 | 修订内容 | 验收方式 |
|---|---|---|---|
| **R7** | Tester（Security、Architect 附议） | ① 写入 PRD 的任何 grep/统计证据**必须附原始命令与输出**并标注取证时间；② E7 契约中"尚未实现"的条款须在 SKILL.md 标注**生效版本**（不得让文档承诺超前于代码） | PRD §3 F1 已按实测修正（11 行）；SKILL.md 契约条款含生效标注 |
| **R8** | Architect（Coder、DevOps 附议） | **W0 启动前必须关闭 D5**（F4 接线位置）；**D7 若确认自证盲区，须在 W0 内闭环**，不得后移 | D5 结论落档；D7 核实结论 + 判据（先定义后核实）落档 |
| **R9** | PM | 澄清 E9/E10 的 P2 = **"论证优先级 P2"，不构成交付承诺**；W3 标注为**条件波次**；E1 的 `--preview` 脱敏与汇总策略**从评审意见提升为 PRD 交付内容** | PRD §4 E9/E10、§6 W3、§4 E1 文本修正 |

### 第三轮评审方式的诚实声明

- 本评价由**宿主 LLM 按 DevSquad 文档化的 prompt-orchestration 模型**执行（未调用真实 LLM backend，与第一轮一致）
- **评分与意见由我按各角色职责边界产出**，非 7 个独立进程的独立输出；**但本轮证据核查是可复现的**（F1 的 grep 结论可由任何人重跑验证）
- 与之对照：第一轮评审**未发现 F1 证据错误**，说明"多角色评分"本身不能替代"证据复核" —— 这一条已写入预防措施

---

## 第四轮：D5 定档取证 + W0 范围变更（R10，2026-09-21）

### 触发

用户下达"按这个方案开始，按项目生命周期推进 DevSquad 的 4.5.20"，进入 W0 执行。按 R8 规定，**D5（F4 接线位置）是 W0-3 开工硬前置**，故先执行 D5 定档取证。

### 取证结论（本 PRD 的关键反转）

**F4 的性质比"孤儿函数"更严重 —— 是"契约级幽灵"**：

| 文档宣称的触发条件 | 代码事实 | 原始证据 |
|---|---|---|
| `mode="review"` | **该 mode 不存在** | `MODES = ["auto","parallel","sequential","consensus"]`（[cli_utils.py:59](../../scripts/cli_utils.py)）；`ALL_DISPATCH_MODES = {"auto","parallel","sequential","consensus"}`（[dispatch_rbac.py:103](../../scripts/collaboration/dispatch_rbac.py)）；[dispatcher.py:437](../../scripts/collaboration/dispatcher.py) docstring 同 |
| `files`（changeset） | **dispatch 无此输入** | [dispatcher.py:420-431](../../scripts/collaboration/dispatcher.py) 签名无 `files`/`changeset`；`grep changeset` 仅命中 `file_bundler.py` 注释与伪 e2e |
| 唯一入口 `Coordinator.apply_file_bundling()` | **全仓零生产调用方** | grep 仅 2 处：定义处 + `V4.5.0_RELEASE_NOTES.md` 表格 |
| 反幽灵门禁的 `counter > 0` | 门禁**自调用**即可满足 | [check_module_activation.py:364](../../scripts/check_module_activation.py) `from ... import FileBundler` → `:450` `FileBundler().bundle([...])` |

**推论**：`mode` 与 `files` 两个入参在整条 dispatch 管线里**都没有来源**。因此"接通"不是接一根线，而是要先造出：新 mode → CLI/RBAC 放行 → changeset 输入契约 → bundle 消费路径 → 可观测输出。**这是新功能，不是修复。**

> 补充证据：`V4.5.0_RELEASE_NOTES.md:96` 曾声称"**5 integration gaps were found and fixed** … All gaps now resolved"，而 FileBundler 正是其中之一 —— 该"已修复"结论**未被验收**，属**发布说明超前于事实**的又一实例（与 R7 同类）。

### 用户决议

**W0 直接接通，接受 W0 由 PATCH 升级为 MINOR。**

### 强制修订（R10）

| 编号 | 内容 | 落点 |
|---|---|---|
| R10-1 | W0-3 拆为 a–f 六项（mode 放行 / `changeset` 参数 / prepare_execution 分流 / bundle 计划方法 / 可观测输出 / 修正伪 e2e） | PRD §6 W0 表 |
| R10-2 | **W0 SemVer 判定由 PATCH 改为 MINOR**；§7.1 判定表作废原首行；§7.2 表同步；release notes 须标注两项对外变化 | PRD §7.1 / §7.2 |
| R10-3 | 冻结定档结论：接线点 = `prepare_execution()`；**复用**既有 `>5 文件` 硬守卫（不新造阈值）；输入仅经 `changeset` 参数传递，**不引入全局配置/环境变量** | PRD §6【D5 定档结论】 |
| R10-4 | W0-3f 升为 F4 验收前提：伪 e2e 未改造为"必经 `dispatch()`"则 W0-3 视为未完成 | PRD §6 R8 前置条件第 4 条 |

### 诚实声明

本轮**不是** 7-Role 评分轮次，而是**执行前的前置条件取证**。取证以 grep + 文件读取为据，原始命令与命中行号已落在 PRD §6【D5 定档结论】。**未重新召集 7 角色评分** —— 范围变更（PATCH→MINOR）已由用户直接裁定，无需评分背书。

---

## 第五轮：W0 执行 + D7 闭环（R11，2026-09-21）

### 触发

R10 定档后 W0-3 开工。R8 前置条件第 2 条规定 **D7（反幽灵门禁自证盲区）必须在 W0 内闭环**，故 W0-1/W0-2/W0-3 完成后立即执行 D7 核实。

### 交付汇总（W0-1 ~ W0-6）

| 项 | 结论 | 验收证据 |
|---|---|---|
| W0-1 | 11 处漂移（8 `version` + 3 `version_source`）全部对齐 SSOT `4.5.19` | `grep -n '^version:' skills/*/skill-manifest.yaml` 8/8 为 4.5.19；旧值 `3.7.2`/`3.9.2`/`4.2.1` 无残留 |
| W0-2 | `check_version_consistency.py` 新增 `_check_sub_skill_manifests()`，项数**运行时读取** | `73 passed, 4 skipped, 0 failed (out of 77 checks)`，EXIT=0；负向测试篡改 manifest → `main()` 返回 1 |
| W0-3 a–f | F4 接通，`apply_file_bundling` **不再有零调用方** | 真实 `dispatch(mode="review", changeset=6 文件)` → 3 bundle；`counter 0 → 1`；非 review / 无 changeset → `review_bundles is None`（V4.5.19 行为保持） |
| W0-4 | **D7 确认成立**：盲区非偶发，是门禁**设计缺陷**；区分机制已交付并双向验证 | 见下 |
| W0-6 | **F4 交付要求 4 落地**：测试名声称"激活/接线"者必须到达生产入口点（AST 判定，文档串不算证据） | 真实 `tests/e2e` 树 0 违规；F4 原始形态合成用例被判违规；`dispatch` 版本不判违规；文档串提及仍判违规；伪造违规时 `main()` 返回 1 |
| W0-5 | 全量回归：除 2 个**纯外网依赖**项外 0 失败（9550 passed）；两个门禁 EXIT=0；lint 无阻塞 | unit `7919 passed, 2 failed`（`UNIT_EXIT=1`，失败项与本轮变更集零交集）；e2e+integration `1631 passed`（`E2E_EXIT=0`）；`73 passed/EXIT=0`；`8/8 production-verified/EXIT=0`；ruff `All checks passed`；radon 无 D 级。**字面"全绿"须由 CI 判定**——详见 PRD §6【W0-5 回归记录】 |

### D7 结论（性质修正）

**原假设（R8）**："反幽灵门禁**可能**存在自证盲区（影响面未知）"，风险等级定为"高（若确认）"。

**核实后的准确表述**：

1. **盲区成立，且是结构性的**：`check_module_activation.py` 的 `main()` 在跑任何生产管线**之前**，就以 `TaskScaleGate().decide(...)` / `_activate_v4513_modules()` 等**直接调用**把全部 30+ 个计数器 bump 完毕，之后才断言 `counter > 0`。**该断言对每一个模块都是自证式的** —— `FileBundler_V450` 绿灯 19 个版本而 `apply_file_bundling` 零调用方，不是"漏网"，而是这一结构的**必然产物**。这使 F4 的定性从"孤儿函数"进一步升级为"**门禁失效条件下的孤儿函数**"。
2. **影响面比预期窄**：抽查 8 个"文档声明位于 dispatch 管线内"的模块，在**跳过门禁、只跑真实 dispatch** 的 fresh 进程实验中，生产 delta **全部 > 0**。即当期**不存在第二个已发生的幽灵**，盲区的实际危害是**假阴性风险**（未来新幽灵无法被抓住），而非历史遗漏。**风险等级由"高"下调为"中"。**
3. **顺带纠正**：`ConnectorFramework`（+6）、`ApprovalGate`（+6）经实测**确为真接线**，非幽灵。

### 交付的区分机制（W0 内闭环）

`check_module_activation.py`：`PRODUCTION_WIRED_COUNTERS` + `_production_probe()`

- **探针前置**：在**所有**自调用之前先执行真实 `MultiAgentDispatcher.dispatch()`（`auto` + `review` 带 6 文件 changeset），记录计数器 delta
- **判据替换**：集合内模块必须由**探针**触发，否则 `FAIL (self-call only)` 且 `return 1`；集合外模块保留原语义但标注 `PASS (self-call)`，**报告不再暗示不存在的生产证据**
- **集合成员**（8 个，均为实测有生产路径者）：DoraMetricsCollector / GapAnalyzer / ErrorBudgetTracker / RiskRegister / ScratchpadHistoryStore / ApprovalGate / ConnectorFramework / FileBundler

**双向验证（缺一不可）**：

| 方向 | 操作 | 期望 | 实测 |
|---|---|---|---|
| 负向 | 把实测 delta=0 的 `MokaAIBackend_P12.1.1` 谎报进集合 | rc=1，且报出该模块 | ✅ `FAIL (self-call only)`，rc=1 |
| 正向回退 | 强制 `prepare_execution(changeset=None)`，**复现 V4.5.19 的 F4 幽灵状态** | rc=1，且报出 `FileBundler_V450` | ✅ 精确报出 `FileBundler_V450`，rc=1 |

> **正向回退是决定性证据**：旧门禁在**同一场景**下会 PASS（自证）。新门禁报红即证明判据非空洞。

永久回归测试：`tests/test_check_module_activation.py`（10 个，含 `T3_F4Regression` + `T4_ActivationClaimDiscipline`）→ `10 passed`。

### W0-6：测试侧的同一失效模式（F4 交付要求 4）

D7 修的是**计数器**的证据等级；W0-6 修的是**测试名**的证据等级。F4 能藏 19 个版本的直接原因是 `test_v450_features_e2e.py` 的函数名叫 `..._activates_in_review_mode`，函数体却直接 `import FileBundler`——**"e2e" 是名字给的，不是路径给的。**

落地方式：并入既有反幽灵门禁（不新建文件），新增 `find_unbacked_activation_claims()`，判据为 **AST 名字/调用节点**（文档串里写 `dispatch()` **不构成证据**），覆盖 `tests/e2e/**` 中名字声称激活/接线的 `test_*`。

| 方向 | 操作 | 期望 | 实测 |
|---|---|---|---|
| 正向 | 扫描真实 `tests/e2e` 树 | 0 违规 | ✅ `violations: 0` |
| 负向 A | 合成 F4 原始形态（声称激活 + 直接 import 模块） | 判违规 | ✅ 1 违规，函数名精确报出 |
| 负向 B | 函数体改为 `dispatcher.dispatch(...)` | 不判违规 | ✅ 0 违规 |
| 负向 C | `dispatch()` 只写进文档串 | 仍判违规 | ✅ 1 违规 |
| 端到端 | `main()` 遇违规 | 返回 1 | ✅ rc=1 且打印 `UNBACKED ACTIVATION CLAIMS` |

**边界（诚实声明）**：判据是**名字驱动**的——故意起平凡名字的伪 e2e 扫不到。本条只覆盖"声称了却没兑现"（F4 的实际形态），"没声称也没兑现"由 D7 的 `PRODUCTION_WIRED_COUNTERS` 负责，两者互补。

### 强制修订（R11）

| 编号 | 内容 | 落点 |
|---|---|---|
| R11-1 | D7 状态由"待核实"改为"已关闭"；风险等级由"高"下调为"中"；补充结构性结论 | PRD §8 D7 段 + §8 风险表 |
| R11-2 | 新增【W0 实施记录】章节，W0-1~W0-4 的真实命令输出入档 | PRD §6 |
| R11-3 | 记录遗留项：集合外 27 个计数器仍为自证式（报告已标注），**逐项接入生产探针为独立工作项，不在 W0 范围** | PRD §8 D7 段 |
| R11-4 | 预防措施第 10 条"前置项未闭合不得开工"新增**判据定义先于核实**要求（D7 教训） | 本文件"预防措施" |
| R11-5 | F4 交付要求 4 独立成 W0-6 并交付（门禁扩展 + 5 个测试 + 真实命令输出入档） | PRD §6 W0 表新增 W0-6 行 +【W0-6 实施记录】 |
| R11-6 | W0-5 全量回归按 CI 权威定义两段式执行，结果如实入档；**2 个外网依赖失败不改测试不改源码**，字面"全绿"判定交由 CI | PRD §6【W0-5 回归记录】 |
| R11-7 | 由 W0-5 的真实 CLI 实测**证伪 PRD 自身的"6+ 文件 → ≥2 bundle"**，按惯例标注更正并同步代码 docstring/help 文案 | PRD §6 W0-3c 行 +【W0 实施记录】；`dispatcher.py` / `cli.py` |
| R11-8 | 落实 R4：创建独立交付分支 `v4.5.20-patch`（改动尚未提交，分支仅指针切换、工作区未受影响） | 本地 git 分支 |

### 诚实声明

1. 本轮**不是** 7-Role 评分轮次，是 **W0 执行的证据回写 + D7 前置项闭环**。**未重新召集 7 角色评分**。
2. 所有结论均附**真实命令输出**（见 PRD §6【W0 实施记录】），无自评式结论。
3. 过程中的一次**误读已自我纠正并留痕**：在 D7 负向实验首轮，我误将"不在 `PRODUCTION_WIRED_COUNTERS` 集合内的模块"读作"已测量且 delta=0"，据此一度以为 `ConnectorFramework` 是第二个幽灵。实际是**集合外模块根本未被测量**。已改为对候选模块做独立测量（`/tmp/d7_candidates.py`）才下结论 —— 此法与 R9 的"多角色评分不能替代证据复核"同源。
4. **W0-3 的接线范围**：仅接通 `mode="review"` + `changeset`；未新增全局配置、未读环境变量、未改动 `>5` 阈值（严守 R10-3 冻结结论）。
5. **已知边界（须在发布说明中如实披露）**：
   - `--dry-run` 时 `prepare_execution()` 不被调用（`validate_roles_and_security` 提前 early_return），故 `--mode review --dry-run --changeset` **看不到 bundle 划分**
   - MCP `multiagent_dispatch` 未暴露 `changeset`，故经 MCP 的 `mode=review` **退化为普通计划**
6. **回归抓到自身缺陷（已修，留痕）**：区分机制初版只判"生产 delta > 0"，**丢掉了原有的 `counter` 非正值 fail-closed 契约**（L-V454-004）。既有 e2e `test_v454_anti_ghost.py::test_full_anti_ghost_gate_exit_codes`（强制 `counter = -100`）在首轮全量回归中**抓红**。**处置为修源码而非改测试** —— 判据改为 `delta > 0` **且** `count > 0`。此例证明"测试是质量守门人"的纪律确实拦下了一次真实的门禁回退。
7. **W0-5 回归范围与结果，以及 W0-6 的交付**：见 `【W0-5 回归记录】` 与 `【W0-6 实施记录】`（PRD §6）—— 均含真实命令输出。汇总：unit 段 `7919 passed, 2 failed`（`UNIT_EXIT=1`）；e2e+integration 段 `1631 passed`（`E2E_EXIT=0`）；`check_version_consistency.py` `73 passed / EXIT=0`；`check_module_activation.py` `8/8 production-verified / EXIT=0`；ruff `All checks passed`；`radon cc scripts/` 无 D 级及以上。
   - **那 2 个失败不得被掩盖，也不得被"修掉"**：`tests/external/test_real_llm.py` 的 `TestErrorHandling` 两个用例**不带 key fixture**，直接对 `api.openai.com` 发真实请求；本机沙箱 `socket.create_connection` 挂起 → `pytest-timeout` 判失败。受控复现（单独跑该文件）为 `2 failed, 3 passed, 18 skipped in 50.21s`；`git status --short tests/external` **输出为空**（与本轮变更集零交集）。**结论：环境依赖，非代码缺陷 → 不改测试、不改源码**；W0-5 的字面验收"`pytest` 全绿"在本机**不可达**，**最终判定必须由 CI 给出**。
   - 因此本记录**不写"全量回归全绿"**——写的是"除 2 个纯外网依赖项外 0 失败（9550 passed）"。
8. **W0-6 的覆盖率诚实交代**：本条是**启发式门禁**（名字驱动），只能覆盖"声称了却没兑现"这一类。它**不能**替代 D7 的计数器证据等级检查，也**不能**证明当前 e2e 树中不存在"没声称也没兑现"的伪 e2e。把它写成"e2e 测试纪律已完全解决"会是过度声明——本记录不这样写。
9. **W0-5 过程中发现并更正了一处 PRD 自身的错误表述（留痕）**：W0-3c 的原验收写作"6+ 文件 → ≥2 bundle"。**真实 CLI 用户路径实测将其证伪**：8 个文件全放在 `scripts/` 时 bundle 数 = **1**（`FileBundler` 规则是"Same directory → same bundle"，[file_bundler.py:51](../../scripts/collaboration/file_bundler.py)），跨 3 个目录时才是 3。**结论：bundle 数由目录/import 结构决定，不由文件数决定。** 处置：① PRD W0-3c 行按项目惯例以删除线 + 【W0-5 实测更正】标注；② `dispatcher.dispatch()` 的 `changeset` docstring 与 `cli.py --changeset` help 文案同步改为"engage deterministic bundling (grouped by directory + imports)"。此例再次印证 R7/R9 的立场：**引用在 PRD 里的数字/断言同样需要被实测复核**。

---

## 第六轮：W0 发布（2026-09-22）

### 触发

用户指令「按项目生命周期继续推进」——W0 的实现与验证已完成（第五轮），本轮进入**发布准备与发布**阶段。

### 事实记录（含真实命令输出，命令见 PRD §6【W0 发布记录】）

| # | 事项 | 结果 |
|---|---|---|
| 1 | 版本原子升级 | **4.5.19 → 4.5.20**（MINOR，R10 判定）；24 个 file spec 全部 PASS |
| 2 | 子技能 manifest 版本字段 | 11 处（8 `version` + 3 `version_source`）已对齐当前版本，即 W0-1 的收口对象在发布态复核通过 |
| 3 | TRAE 三层缓存 | L1/L2/L3 × 2 文件 = 6 项 `cmp` 全部 same；门禁 3 个 content-diff pair 全 PASS |
| 4 | 批量替换后人工复核 | 残留 `4.5.19` 共 75 处 / 19 文件，逐条确认为**历史引用或行为对照**，均不该动 |
| 5 | 发布说明 | 新建 `docs/release_notes/V4.5.20_RELEASE_NOTES.md`（R10-2 落实物） |
| 6 | 对外文档错误表述修正 | **6 份**：`CHANGELOG.md` / `README.md` / `README-CN.md` / `README-JP.md` / `EXAMPLES.md` / `INSTALL.md` |
| 7 | `check_version_consistency.py` | `73 passed, 4 skipped, 0 failed (out of 77 checks)`；`Version 4.5.20 is consistent.`；**EXIT=0** |
| 8 | `check_module_activation.py` | `8/8 production-verified, 27 self-call verified`；`FileBundler_V450 counter=2 [PASS (production +1)]`；**EXIT=0** |
| 9 | `ruff check . --ignore=E501` | `All checks passed!`；**EXIT=0** |
| 10 | `radon cc scripts/ -nd -s` | 无 D 级及以上 |
| 11 | unit 段（升级后） | `2 failed, 7919 passed, 25 skipped, 6 deselected in 451.05s`；2 failed 仍为 `tests/external` 纯外网依赖项 |
| 12 | e2e + integration 段（升级后） | `1631 passed, 81 deselected in 403.71s`；**EXIT=0** |

### 强制修订落实（R10-2 与 R11-6/7/8 结项）

| 编号 | 原内容 | 状态 | 落地证据 |
|---|---|---|---|
| **R10-2** | W0 版本判定改 MINOR；**release notes 必须显式标注两项对外变化**，不得静默变更 | ✅ **已落实（本轮）** | [V4.5.20_RELEASE_NOTES.md](../release_notes/V4.5.20_RELEASE_NOTES.md) 的「⚠️ Read This First — Two Outward-Facing Changes」：① 新 `mode="review"` + `changeset` 契约（含 before/after 表 + CLI/Python 示例 + 向后兼容声明）；② >5 文件的 bundle 行为变化，按**实测口径**表述（8 文件跨 3 目录 = 3 bundle；8 文件同目录 = 1 bundle），并明确写出"more than 5 files ⇒ multiple bundles"是**错误**命题 |
| **R11-6** | W0-5 回归按 CI 权威定义两段式执行、如实入档；2 个外网依赖失败不改测试不改源码 | ✅ **已结项（并复跑于升级后）** | PRD §6【W0-5 回归记录】+【W0 发布记录】(5)；两段命令与 CI `.github/workflows/test.yml` 逐字一致 |
| **R11-7** | 证伪 PRD 自身"6+ 文件 → ≥2 bundle"并同步代码文案 | ✅ **已结项，且范围扩展到 6 份对外文档** | PRD W0-3c 行删除线更正；`dispatcher.py` / `cli.py` 文案；另修正 `CHANGELOG.md` / `README.md` / `README-CN.md` / `README-JP.md` / `EXAMPLES.md` / `INSTALL.md` |
| **R11-8** | 落实 R4：创建独立交付分支 `v4.5.20-patch` | ✅ **已结项** | 当前分支即为 `v4.5.20-patch`；48 个变更文件在该分支工作区（提交待用户确认） |

### 诚实声明（第六轮）

1. 本轮**不是** 7-Role 评分轮次，是 **W0 的发布执行与文档回写**。**未重新召集 7 角色评分。**
2. 所有结论均附真实命令输出（PRD §6【W0 发布记录】），无自评式结论。
3. **不写"全量回归全绿"**：unit 段的 `UNIT_EXIT=1` 来自 `tests/external/test_real_llm.py` 的 2 个纯外网依赖项（沙箱 `socket.create_connection` 挂起 → `pytest-timeout`）。**既不修改测试、也不修改源码**；字面全绿的判定**交由 CI**（CI 有出网能力，请求返回 401 使断言成立）。本记录写的是"除 2 个纯外网依赖项外 0 失败（9550 passed）"。
4. **升级前后回归数值一致**（7919 / 1631）——这是符合预期的（本次变更仅动版本字符串与文档），**不是因为没跑**。
5. 主动发现的行为：`check_version_consistency.py` 的 SKILL.md frontmatter 检查依赖 `PyYAML`；系统 `python3` 未装该包时会报 `ModuleNotFoundError: No module named 'yaml'` 并判 FAIL。**这是环境差异而非门禁缺陷**（CI 环境装了依赖）。发布校验统一使用 `~/.pyenv/versions/3.12.13/bin/python3`，**不用系统 python3**。
6. 过程复盘（工程教训留痕）：用 `cmd | tail -N` 形式取末尾输出时，`$?` 会变成 `tail` 的退出码，**必须用 `PIPESTATUS[0]`**，否则会得到假绿。本轮首次取门禁退出码即犯此错（`VERSION_GATE_EXIT=0` 实际来自 `tail`），已改用 `set -o pipefail` 重跑取得真值。此条与项目既有教训"CI 中 `pytest | tee` 必须 `set -o pipefail`"同源。

### 待用户裁定

- **提交信息与是否推送**：按项目纪律"未获明确要求不提交/不推送"，本轮**未执行任何 `git commit` / `git push` / `git tag`**。分支 `v4.5.20-patch` 工作区含 48 个变更文件待提交。
- W1–W3（P1/P2）未开工；`docs/prd` 中其他历史 mode 列表残留项暂不改。

**【已裁定（用户 2026-09-22）】** 提交方式 = **分两个提交**；推送 = **推送分支 + push tag `v4.5.20`**。两项均已执行，结果见第七轮。

---

## 第七轮：发布后缺陷登记与 P0 处置（2026-09-22）

### 触发

用户指令「按项目生命周期，接下来做什么，还有哪些未解决」+ 裁决：「**先落文档，anyio+flaky 一起修**」「**先清 P0 再进 W1**」。本轮为 W0 发布后的**缺陷登记与处置**轮次。

### 事实记录（含真实命令输出，命令见 PRD §6【发布后缺陷登记与处置】）

| # | 事项 | 结果 |
|---|---|---|
| 1 | 提交①（产品/门禁真相） | `f867294` — 40 files changed, 1062 insertions(+), 114 deletions(-) |
| 2 | 提交②（release notes + 文档回写） | `01ea836` — 8 files changed, 1774 insertions(+), 5 deletions(-) |
| 3 | 推送 | `[new branch] v4.5.20-patch -> v4.5.20-patch`（含 tracking）；`[new tag] v4.5.20 -> v4.5.20`（annotated） |
| 4 | 一致性校验 | `HEAD = origin/v4.5.20-patch = 01ea836`；`v4.5.20^{commit} = 01ea836`；提交后工作区变更数 = 0 |
| 5 | `CI` run `35687266001`（tag） | `deployment` ✓ / `test (3.10)(3.11)(3.12)` ✓ / `e2e` ✓ / `lint` ✓ / **`security` ✗（唯一红）** / `performance`·`build` skipped |
| 6 | `Release` run `35687266218`（tag） | Final Release Gate **✗**（红在 `Run pip-audit hard check`）；`Publish to PyPI`、`Create GitHub Release` skipped |
| 7 | **连带门禁真空** | 该 job 的步骤 **11–16 全部 `skipped`**：Docker/Compose、Helm lint、Validate Helm chart、bandit、dispatcher 体积、测试计划一致性 —— **发布态未知，不得记作通过** |
| 8 | P0-1 根因 | `requirements.lock:21 anyio==4.13.0`；`pyproject.toml` 未钉 anyio → 红项**只来自 lock 被显式安装** |
| 9 | P0-1 本地复现 | `pip-audit -r requirements.lock --strict` → `Found 2 known vulnerabilities in 1 package`（CVE-2026-63374 / CVE-2026-64847，fix 4.14.2）；`PIP_AUDIT_LOCK_EXIT=1` |
| 10 | P1-1 证据（夜间 run `35574509278`） | `test (3.11)` ✗ `AssertionError: 3823.5640409999974 not less than 1000.0 : 200 writes too slow: 3823.6ms`；`test (3.10)`·`test (3.12)` **cancelled**（fail-fast 连带）；`security` ✗ |
| 11 | P1-1 间歇性判定 | 同一测试在 tag run `35687266001` 的 3.10/3.11/3.12 **全部 success** → 间歇性时序抖动，非确定性缺陷 |
| 12 | P1-1 本机基线（为设定判据而测） | 5 次连续 200 writes：`[88.4, 90.3, 90.4, 91.0, 95.5]`，**median 90.4 ms** → 与 CI 3823.6 ms 相差约 **42×** |
| 13 | **正面结论（关闭 W0-5 悬置项）** | tag run 功能类 job 在 GHA 出网环境**全绿** → 第六轮对 `tests/external` 2 项本机失败的"纯环境依赖"判定**被证实** |
| 14 | P2-1 发现 | 接通 `.env` 的 DeepSeek 配置后本机单元段出现 `1 failed`：`test_get_client_omits_base_url_when_absent`，断言值为 `https://api.deepseek.com/v1`（= `.env` 的值）→ 定位为 **`conftest.py::_load_env_file()` 环境注入 + 测试未隔离** |
| 15 | P2-1 受控实验 | A) 保留 `.env`：`1 failed, 67 passed`；B) 同文件 + `OPENAI_*`/`DEVSQUAD_OPENAI_*`/`MOKA_*` 置空：`68 passed` → **与 P0-1/P1-1 修复无关** |
| 16 | P2-1 处置 + 双向校验 | 已加 `monkeypatch.delenv("DEVSQUAD_OPENAI_BASE_URL")`；正向 `68 passed`；**负向注入 `base_url='https://injected/v1'` 仍变红** → 隔离未架空测试 |
| 17 | **P2-2 新发现（待裁定）** | `deepseek-flash` 为**推理模型**，`reasoning_content` 独立但**推理 token 计入 `max_tokens`**；探针实测 `max_tokens=100` → `finish='length' content_len=0 reasoning_len=489` → `generate()` **静默返回空串**，`FallbackBackend` 把空串当成功 → 静默失败模式 |
| 18 | **P2-3 新发现（待裁定）** | 本机 `.env` 含 `MOKA_API_KEY`/`MOKA_API_BASE`（第三方中转）→ `create_backend("auto")` 实为**三元链**（OpenAI→Moka→Mock），与 `test_auto_with_openai_key_uses_real_backend` 的"二元链"前提不符（`assert 3 == 2`） |
| 19 | **P2-2 裁决与实施** | 用户裁决 = **「先只加告警日志，不改返回行为」**；已在 `OpenAIBackend.generate()` 实现（`finish_reason=='length'` 且内容为空 → WARNING，含 model/max_tokens），**返回值仍为 `""`**；新增 2 条单测（告警必发 / 有内容时不发） |
| 20 | P2-2 负向对照（证明新测试有牙齿） | 仅静音 `llm_backend` 的 logger（临时插件 `/tmp/silence_llm_logger.py`，不入库）→ 新测试**变红** `assert 0 == 1`；同批另一条失败是该插件对既有日志测试的连带影响 |
| 21 | 全量段复跑（P0+P1+P2-1 修复后、P2-2 实施前） | `pytest tests/unit/ tests/integration/ tests/contract/ tests/test_*.py tests/security/ --ignore=tests/e2e --ignore=tests/external -q -p no:randomly` → **`9343 passed, 2 skipped` / `SUITE_EXIT=0`**（403.10s） |
| 22 | **P2-4 新发现（待裁定）** | 同一 `.env` 下 P2-2 后复跑全量段 → `2 failed, 9343 passed, 2 skipped`（447.74s）：`test_dispatch_autonomous_works_when_enabled` 撞 `--timeout=60`（3 次真实角色投票各 401 + 退避）、`test_concurrent_dispatch_stability` 由 `anchor_checker` drift 判失败。**两项在隔离运行时均通过**（`2 passed`，78.05s） |
| 23 | P2-4 受控实验（决定性） | A) provider key 置空 → `2 passed in 1.42s`；B) 保留 `.env` → `2 passed in 78.05s`（**55×**）→ 带 key 即走真实 LLM 路径。P2-2 新增分支（`finish_reason=='length'` 且内容为空）在两条失败路径上**均未被触发**（401 在调用阶段即抛出；另一条的下游 `summary` 非空） |
| 24 | **CI 等价全量段（= 本轮发布判据）** | `env OPENAI_API_KEY= DEVSQUAD_OPENAI_API_KEY= MOKA_API_KEY= DEVSQUAD_ANTHROPIC_API_KEY= ANTHROPIC_API_KEY= pytest …` → **`9345 passed, 2 skipped` / `SUITE3_EXIT=0`**（177.52s）。`9345 = 9343 + 2`（P2-2 新增）→ 无测试被静默吞掉；耗时长差（177.52s vs 447.74s）即真实 LLM 网络等待 |

### 诚实声明（第七轮）

1. 本轮**不是** 7-Role 评分轮次，是发布后缺陷登记与 P0 处置。**未重新召集 7 角色评分。**
2. **不写"发布已成功"**：`v4.5.20` 的 tag 已推送、制品分支已就位，但 `Release` 工作流**未完成 M4**（PyPI 发布与 GitHub Release 均 skipped），且 6 项发布门禁从未执行。本记录写的是"tag 已推、发布门禁**未闭合**"。
3. **不写"全量回归全绿"**：本机 `tests/external` 2 项在 `.env` 接通真实 key 后由 skipped 变为实际执行并失败。**第六轮把它们判为"纯环境依赖"的结论，本轮被证伪一半**（见第 17/18 条）：`test_auto_with_openai_key_uses_real_backend` 的 `assert 3 == 2` 是**本机 `.env` 的 `MOKA_*` 使 `auto` 链变三元链**（环境前提不符），而 `test_generate_simple`/`test_openai_multi_turn` 的空串是**推理模型预算耗尽导致的产品侧静默空返回**（非"外网抖动"）。CI 侧的唯一红仍是 P0-1。
3b. **推测不得当结论**：第六轮"对外网依赖"的解释当时**未做原始探针**即写入文档。本轮以 `finish_reason` / `reasoning_tokens` 原始字段复现后才定性（R11 第 12 条：判据先于核实，且要能证伪）。
4. P1-1 的性质判定以**实测比率**为依据（本机 90.4 ms vs CI 3823.6 ms），**不是主观调参**；判定为"测试判据设计缺陷"，处置须在提交信息与 PRD 本节写明"修的是测试设计，不是放阈"，并**用受控实验证明新判据仍能抓真实退化**（R11 预防措施第 13 条）。
5. 本轮沿用第六轮教训：取退出码一律 `set -o pipefail`（或写临时文件后取 `$?`），**不用 `cmd | tail` + `$?`** —— 本轮复核 P0-1 时首次仍得到假绿 `PIP_AUDIT_LOCK_EXIT=0`（tail 的退出码），已重取真值 `=1`。该错误**连续两轮复现**，已升级为**必须复述的检查项**。

### 待用户裁定（第七轮）

- 本轮已获裁决并执行：P0 处置顺序 = **先落文档 → anyio 与 flaky 一起修**；W1 时机 = **先清 P0 再进 W1**。
- 本轮已获裁决并执行：推送方式 = **提交 + 推分支 `v4.5.20-patch` + 开 PR → `main`**（**不打新 tag**）。CI 的 `push` 仅在 `main` 与 `v*` tag 触发，故验证 `security` job 转绿的唯一途径是 PR 事件。
- **【已裁定并实施，P2-2】** 用户裁决 = **「先只加告警日志，不改返回行为」**（第 19/20 条）。 **【第八轮改判：该裁决已被推翻 —— 新裁决 =「维护告警 + 按模型抬高预算 + 空串不再当成功」；原裁决基于不完整证据（见第八轮）】**
- **【仍待裁定，P2-3】** 是否按 P2-1 同法为 `test_auto_with_openai_key_uses_real_backend` 显式隔离 `MOKA_*`，并清理本机 `.env` 的 Moka 条目？ **【第八轮已裁定：顺序不动、`.env` 也不动；Moka Key 实测 401，登记为"已知并接受的行为"（见第八轮）】**
- **【仍待裁定，P2-4】** 本机带真实 key 时，全量段有 2 项走真实 LLM 路径（隔离跑通过、并发跑撞 `--timeout=60`/drift）。选 ① 为这两项显式隔离 provider key（与 P2-1 同法），还是 ② 把"键置空的 CI 等价命令"固定为本地验收入口并写进测试文档？（**不影响 CI**，CI 无 key 恒走 mock）
- **【仍待裁定】新 tag**：`v4.5.20` 的 Release 工作流后半段 6 项门禁从未执行。是否打新 tag（`v4.5.20` 重打 / `v4.5.21`）以触发 `release-e2e.yml` 验证？**注意 tag 是发布动作**，未裁定前不执行。**【第八轮已裁定：不切 tag，先收尾补丁；版本位归属（PATCH `4.5.21` vs MINOR `4.6.0`）未定】****【第九轮已裁定：不得升 MINOR —— 维持当前 Minor 版本 `4.5`，任何 MINOR 升级须用户显式批准】**

---

## 第八轮：auto 链顺序反转 + 推理预算契约收紧 + size gate 拆文件（2026-09-23）

### 触发

第七轮之后的连续用户裁决：① auto 链顺序；② 推理预算/空串返回契约（推翻第七轮的 report-only）；③ dispatcher size gate 处置；④ Moka 401 的处理；⑤ 提交策略；⑥ 发版策略。

### 事实记录（含真实命令输出）

| # | 事项 | 结果 |
|---|---|---|
| 1 | **auto 链顺序单点化** | `llm_backend.py` 新增单一有序来源 `API_BACKEND_ORDER: tuple[str, ...] = ("moka", "openai", "anthropic")`；`auto` 与 `auto-fallback` 共用它，`MockBackend` 恒为末位；链构造改为 `for name in API_BACKEND_ORDER`，**不再依赖 `if` 块的书写顺序** |
| 2 | **链解析实测（真实 Key）** | `create_backend("auto")` → `AUTO wrapper=FallbackBackend n=3` / `- MokaAIBackend: model='claude-opus-5'` / `- OpenAIBackend: model='deepseek-flash'` / `- MockBackend: model=None` |
| 3 | 文档旧顺序修正 | `.env.example` 与 `config/deployment.yaml`（原写 `anthropic -> openai`，现为 `moka -> openai -> anthropic`）；`docs/planning/V3_9_2_ROADMAP_PLAN.md` 属历史文档，**未改** |
| 4 | **`generate()` 返回契约收紧（BREAKING）** | `finish_reason == 'length'` **且** content 为空 → 先记 WARNING（含 model 与生效 `max_tokens`）再 `raise RuntimeError`；**content 非空但被截断仍原样返回**，不告警不抛错 |
| 5 | 契约的支撑实测（原文） | `MT=2048 → finish='length', content_len=0, reasoning_len=5058, reasoning_tokens=2048`（2048 预算被 reasoning token **100% 吃光**，content 为空） |
| 6 | **假绿防护（实现细节）** | `RuntimeError` **不在** `_get_openai_retry_exceptions()` = `(ConnectionError, TimeoutError, OSError, APIError)` 内 → 立刻逃逸、**不消耗重试次数**；Prometheus `record_llm_call(..., True)` **已移到空内容检查之后** → 失败调用不被记成成功 |
| 7 | **推理预算按模型抬高** | 新增 `scripts/collaboration/reasoning_budget.py`；`REASONING_MODEL_MARKERS=("deepseek-flash","reasoner","reasoning","-r1","thinking")`；`resolve_max_tokens(model, explicit)` 优先级 = 显式 > `DEVSQUAD_REASONING_MAX_TOKENS` > `DEFAULT_LLM_MAX_TOKENS_REASONING=16384` > `DEFAULT_LLM_MAX_TOKENS=4096` |
| 8 | DeepSeek 端点契约实测 | `base_url=https://api.deepseek.com/v1`；`GET /models` → `['deepseek-flash', 'deepseek-v4-pro']`；`temperature` 被接受；流式可用（`content_chunks=8, reasoning_chunks=24`，reasoning 增量先于 content 到达）；`max_tokens` 可接受至 65536 |
| 9 | **P0-2 CI 红（新发现）** | `release-e2e.yml` run `35696422202`，`V4.5.16 Final Release Gate` step 15 `Run dispatcher size gate` 失败（此前因 pip-audit 红而 skipped，修好 anyio 后才暴露）。输出：`+ 224 949 (was 725) check_module_activation.py` / `+ 97 826 (was 729) check_version_consistency.py` / `+ 62 1008 (was 946) collaboration/coordinator.py` / `+ 17 1240 (was 1223) collaboration/dispatcher.py` |
| 10 | **P0-2 处置（抽取重构，非改基线）** | `check_module_activation.py` 949→**533**（新 `check_module_activation_probes.py` 459 行）；`check_version_consistency.py` 826→**683**（新 `check_version_consistency_content_diff.py` 168 行 + `check_version_consistency_types.py` 17 行）；`coordinator.py` 1008→**796**（新 `coordinator_compression_mixin.py` 246 行，`class Coordinator(CoordinatorCompressionMixin)`）；`dispatcher.py` 1240→**1140**（新 `dispatcher_plugins_mixin.py` 120 行）。基线文件 `docs/audits/dispatcher_size_baseline.json` **未被修改**（`git diff` 为空）。行数为门禁非空行口径 |
| 11 | **P0-2 转绿** | `PASS: dispatcher size gate (no net growth past baseline)`；`SIZE_GATE_EXIT=0` |
| 12 | **Moka 中转 Key 401（已裁定）** | `devsquad doctor` 实跑：`[MOKA] Configured: ✓ configured  Reachable: ✗ unreachable  Error: HTTP 401: Unauthorized` / `[OPENAI] Configured: ✓ configured  Reachable: ✓ reachable (3293ms)  Models: 2 → deepseek-flash, deepseek-v4-pro` / `[ANTHROPIC] Configured: ✗ NOT configured` |
| 13 | 提交策略 | 追加到 **PR #10** 的新 commit（分支 `v4.5.20-patch`，PR 已开） |
| 14 | 发版策略 | **不切 tag，先收尾补丁**；**版本位归属（PATCH `4.5.21` vs MINOR `4.6.0`）未定**。**【第九轮已裁定：不得升 MINOR —— 维持当前 Minor `4.5`，任何 MINOR 升级须用户显式批准】** |
| 15 | **CI 等价全量段** | `9368 passed, 2 skipped, 1 warning in 152.48s (0:02:32)`；**`SUITE_RUN3_EXIT=0`** |
| 16 | 交叉核对 | 上一轮（改动前）`9345 passed, 2 skipped`，本轮 `9368`，差值 **+23** 与新加测试数一致 → 无测试被静默吞掉 |
| 17 | e2e + smoke（Key 置空） | `285 passed, 11 skipped, 1 warning in 226.05s (0:03:46)`；退出码 **0** |
| 18 | **门禁五项** | `SIZE_GATE_EXIT=0`、`VERSION_GATE_EXIT=0`（`73 passed / 4 skipped / 0 failed`）、`RUFF_EXIT=0`、`MYPY_EXIT=0`；另 `ACTIVATION_EXIT=0`（`8/8 production-verified, 27 self-call verified`） |
| 19 | external 真实网络用例 | 真实 Key：`18 passed, 8 skipped`（skip 为 Anthropic 无 Key）；置空环境同批：`100 passed, 24 skipped` |
| 20 | **Task 5.4 真实 Key 端到端探针** | `AUTO wrapper=FallbackBackend n=3` / `- MokaAIBackend: model='claude-opus-5'` / `- OpenAIBackend: model='deepseek-flash'` / `- MockBackend: model=None` / `AUTO_RESULT t=45.97s type=str len=2114  NON_EMPTY=True` / `DIRECT t=4.17s raised RuntimeError: ... (model=deepseek-flash, max_tokens=64)` / `CONTRACT_HELD=empty_length_raised` / `CHAIN_DEGRADED t=2.09s len=242  is_mock_output=True  NON_EMPTY=True` |

### 用户裁决原文（逐条引用）

1. **auto 链顺序**：**「确实要 Moka 优先（按我写的顺序改）」**（其前一句为「你说得对，还是按着先Moka，再deepseek，再Mock的路径吧。」）。
2. **推理预算/返回契约**：**「维护告警 + 按模型抬高预算 + 空串不再当成功（推荐）」**。
3. **dispatcher size gate**：**「纳入本次 spec，拆文件转绿（推荐）」**。
4. **Moka 401**：**「顺序不动、.env 也不动」**。
5. **提交策略**：**「追加到 PR #10 新 commit（推荐）」**。
6. **发版**：**「不切 tag，先收尾补丁（推荐）」**。

### 链顺序反转过程（如实记录，不粉饰）

用户最初的问题是 **「Moka不再用」**（是否要移除 Moka），与随后一句 **「Moka 优先」** **自相矛盾**。**复问后**用户明确要求 **Moka 优先**（第 1 条），故最终实现为 `API_BACKEND_ORDER = ("moka","openai","anthropic")` —— **Moka 排第一，而非被删除**。此反转过程在本记录与本 PRD §6 中如实留痕。

### Moka 401 的定性（明确接受，非遗留）

用户裁决 **「顺序不动、.env 也不动」** → 本项**不再待定**，登记为**已知并接受的行为**：代码顺序保持 `API_BACKEND_ORDER = ("moka","openai","anthropic")` 不变；开发者本机 `.env` 的 `MOKA_*` 条目不清理。**代价（显式告知）**：每个新进程首次调用会先付一次失败请求再降级到 DeepSeek，并产生一条 `FallbackBackend: MokaAIBackend(...) failed (RuntimeError, reason=provider_error), trying next` 的 WARNING 日志；**测试/CI 不受影响**（用 Key 置空环境走 mock 路径）。

### 诚实声明（第八轮）

1. 本轮**不是** 7-Role 评分轮次，是**契约改判 + 重构 + 文档回写**。**未重新召集 7 角色评分。**
2. **上一轮 P2-2 的 report-only 裁决基于不完整证据** —— 第七轮作出"只加告警、不改返回行为"时，**未实测**推理 token 会吃光预算，也**未验证**真实链路的长推理 prompt 真会触发空内容路径。本轮先取证（`MT=2048 → reasoning_tokens=2048, content_len=0`）后才改判。
3. **不写"发布成功"**、**不替用户决定版本位**：本批含 BREAKING（`generate()` 返回契约 + auto 链顺序），版本位归属（PATCH `4.5.21` vs MINOR `4.6.0`）**未定，留待后续单独裁决**。**【第九轮已裁定：不得升 MINOR，维持 `4.5`；PATCH 位是否切 tag 仍待裁定】**
4. **未验证项如实标注**：一次长推理 `generate()` 实测耗时 **45.97s**（`DEFAULT_LLM_TIMEOUT_SECONDS = 120.0`），与更早的 P2-4"多角色调度下 60s 超时"现象**可能相关，但本轮未做归因** —— 记为**未验证**。
5. **链顺序反转过程不自辩**：用户原问"不再用"与后句"要优先"自相矛盾，已在第 1 条与其上文如实记录复问过程。

### 结项与遗留（第八轮）

- **本轮结项**：P0-2（size gate 拆文件转绿，基线与行为均未改）、P2-2（契约改判）、P2-3（Moka 401 已知并接受）；第七轮"是否打新 tag"→ **裁决为不切 tag**。
- **仍待后续裁定**：**版本位归属**（PATCH `4.5.21` vs MINOR `4.6.0`）；**P2-4**（本机带真实 key 时的 60s 超时/ drift 项）未在本轮归因。**【第九轮：版本位已裁定为"不得升 MINOR，维持 `4.5`"】**
- **不静默**：本批 BREAKING 已写入 `CHANGELOG.md` 的 `## [Unreleased]`；`docs/reference/VERSION_HISTORY.md` 按已发布版本号分节，**待版本位判定后再写入**（显式暂缓）。

---

## 第九轮：版本纪律裁定 + mypy 存量错误修复 + W1 准入判定（2026-09-24）

### 触发

第八轮收尾后用户给出三条指令：① 维持当前 Minor 版本，未经显式批准不得升 MINOR；② 修复代码库中 3 个存量 mypy 错误，要求**根因修复而非临时绕过**；③ 复核 CI 与 Release E2E 完整结果，据此判定能否进入 W1 波次。

### 事实记录（含真实命令输出）

| # | 事项 | 结果 |
|---|---|---|
| 1 | **CI 全绿（PR #10 事件触发）** | run `35839990737`：`security: success`、`deployment: success`、`lint: success`、`e2e: success`、`build: success`、`test (3.10)/(3.11)/(3.12): success`；`performance: skipped`（仅 tag 触发，符合预期）。**P0-1 的 anyio 修复在 CI 侧确认生效** |
| 2 | **Release E2E 全绿** | run `35839999897`（`workflow_dispatch` on `v4.5.20-patch`）：3 个 job 全 success —— `Host Bridge E2E`、`Real LLM E2E`、`V4.5.16 Final Release Gate` |
| 3 | **6 项此前 skipped 的门禁首次真实执行且全绿** | steps 10–16 全部 `success`：`pip-audit hard check`(10)、`Docker and Compose checks`(11)、`Helm lint and template checks`(12)、`Validate Helm chart`(13)、`bandit gate`(14)、**`dispatcher size gate`(15)**、**`Validate test plan + baseline doc consistency`(16)**。**step 15 由红转绿，step 16 不再是 skipped** |
| 4 | **3 个 mypy 存量错误（根因修复）** | `config_manager.py:380` redundant-cast → 改为显式局部注解；`i18n.py:117` redundant-cast → 加显式局部注解（见第 5 条）；`dag_views.py:327` `Module has no attribute "mermaid"` → 改为 `getattr(st,"mermaid",None)` + `callable()` 判定。**未使用任何 `type: ignore`、未放宽任何检查** |
| 5 | **两配置冲突的根因（受控实验）** | `mypy` 探针实测：`options[selected_idx]` 在 `selected_idx: Any` 时 reveal 为 **`Any`**（索引被 Any 污染），故不带 site-packages 时报 `no-any-return`；带 site-packages 时 `st.selectbox` 返回 `int` → 索引得 `Locale`，故原 `cast(Locale, ...)` 报 redundant-cast。**同一行在两种配置下要求相反**，显式局部注解同时满足两者 |
| 6 | **修复后双配置全绿** | 带 site-packages：`Success: no issues found in 314 source files`（exit 0）；CI 等价 `--ignore-missing-imports --no-error-summary --no-site-packages`：exit 0；**去掉 `--no-site-packages`（其余同 CI 命令）：exit 0** |
| 7 | **门禁盲区（新发现，本轮的实质收获）** | CI 的 mypy 步骤为 `mypy scripts/ skills/ --ignore-missing-imports --no-error-summary --no-site-packages`。`--no-site-packages` 使第三方 stub 不可见，**恰好掩盖了第 4 条这 3 个错误** —— 即"mypy 门禁绿"不等于"类型正确"，属与 P0-2（size gate 从未执行）同类的**假绿** |
| 8 | **`st.mermaid` 运行时也不存在** | 实测 pinned Streamlit **1.57.0**：`hasattr(st,'mermaid') → False`、`callable(...) → False`。原注释"Streamlit < 1.29 不支持"的**前提未经证实**（1.57.0 远新于 1.29 且仍无此属性）；该分支在当期依赖下**恒走 `st.code` 回退** |
| 9 | **black 全仓既有红（非本批引入）** | `black --check scripts/ tests/` → `450 files would be reformatted, 234 unchanged`。用项目配置（`--config pyproject.toml`, `line-length=120`）对 HEAD 与本批同 3 个文件分别复跑，**两侧结论一致（均 would reformat）** → 既有状态。**注**：首次对比把 HEAD 文件写到 `/tmp` 导致未加载项目配置，结论无效，已用显式 config 重做 |
| 10 | 定向测试 | `pytest tests/ -k "config_manager or i18n or dag_view or locale or dashboard"` → `223 passed, 1 skipped, 9468 deselected`，exit 0 |
| 11 | 门禁复跑 | `ruff check . --ignore=E501` → exit 0（`All checks passed`）；`VERSION_GATE_EXIT=0`；`SIZE_GATE_EXIT=0`；`DOC_TESTS_EXIT=0` |
| 12 | 版本现状 | `VERSION` = **4.5.20**（本批未改版本号） |

### 用户裁决原文（逐条引用）

1. **版本纪律**：**「Maintain the current Minor version unless explicitly approved by me. No Minor version upgrades should be implemented without prior authorization.」** → **MINOR 升级需显式批准；本批维持 `4.5` 线，不升 `4.6.0`**。
2. **mypy 修复**：**「Address and resolve all 3 existing mypy errors in the current codebase as part of this development cycle. Ensure comprehensive fixes that eliminate these specific type checking issues rather than implementing temporary workarounds.」**
3. **W1 准入**：**「Review the complete results of the CI and Release E2E tests. Based on these outcomes, determine whether the current build should proceed to the W1 wave deployment schedule. Document any test failures or concerns that would impact this decision.」**

### 版本纪律的定性（第八轮遗留项的结项）

第八轮把"版本位归属（PATCH `4.5.21` vs MINOR `4.6.0`）"记为**待裁定**；本轮已裁定为 **不得升 MINOR**。故：**本批 2 项 BREAKING 不改变 Minor 位**，`VERSION` 维持 `4.5.20` 不动；是否在 `4.5` 线内切 PATCH tag 仍属发布动作，**未裁定前不执行**。

### W1 准入判定

**结论：准予进入 W1 波次。** 依据为第 1–3 条（CI 8 个 job 全绿 + Release E2E 3 个 job 全绿 + 6 项此前 skipped 的门禁首次真实执行且全绿），**无测试失败**。附带的关注项（不阻塞准入）见下节。

### 诚实声明（第九轮）

1. 本轮**不是** 7-Role 评分轮次，是**版本纪律裁定 + 存量缺陷修复 + 准入判定**。**未重新召集 7 角色评分。**
2. **"mypy 门禁绿"曾被当作类型正确** —— 本轮证明该结论**不成立**（第 7 条）：`--no-site-packages` 掩盖了 3 个真实错误。此前多轮记录中的"MYPY_EXIT=0"仅能证明**该 flag 组合下**无错，**不能**证明类型正确。此为对本项目既有陈述的**诚实更正**。
3. **未擅自改动 CI 门禁**：第 7 条盲区的修复（是否去掉 `--no-site-packages`）**属门禁变更，未获授权，本轮不做** —— 仅登记并给出本地已验证的依据（第 6 条：去掉后 exit 0）。
4. **未擅自扩大范围**：`st.mermaid` 分支在当期依赖下恒走回退（第 8 条），**保留**该防御性探测而未删除分支，因其属"未验证未来版本"的取舍，**留待用户裁定**。
5. **未把推测写成结论**：black 全仓红（第 9 条）**只断言"与本批无关"**（HEAD 与本批同结论），**未断言**其成因（black 版本漂移 vs 格式约定变更），成因**未验证**。

### 结项与遗留（第九轮）

- **本轮结项**：版本纪律裁定（不升 MINOR）、3 个 mypy 存量错误根因修复、W1 准入判定（准予进入）。
- **仍待后续裁定**：① 是否去掉 CI mypy 的 `--no-site-packages` 以关闭假绿盲区；② `st.mermaid` 死分支保留还是删除；③ `4.5` 线内是否切 PATCH tag（发布动作）；④ **P2-4** 未归因（本机带真实 key 的 60s 超时 / drift，与 45.97s 长推理耗时关系未验证）；⑤ black 全仓红的成因与是否纳入门禁。**【第十轮：① 已裁定并完成（去 flag 已落地）；②③④⑤ 状态不变，仍待裁定】**
- **不静默**：本轮对"mypy 曾报绿"的更正已写入第 2 条诚实声明，并同步至 PRD §6。

---

## 第十轮：门禁可信度收口 + 全量时序断言改造 + W1 判定维持（2026-09-24）

### 触发

第九轮把"是否去掉 CI mypy 的 `--no-site-packages`"（假绿盲区）列为**待裁定**。用户本轮先裁定该项，随即批准把 (15) 中"至少还有 9 处"的同类反模式**全量改造**，并批准为完成前者所需的 `python_version` 上抬。三项均属**门禁/判据变更，先请示后执行**；本轮据此实施并复核 W1 准入。

### 事实记录（含真实命令输出）

| # | 事项 | 结果 |
|---|---|---|
| 1 | **授权①去 flag（已落地）** | `--no-site-packages` 从全部 **3 处** mypy 步骤移除（`test.yml` 两步 + `release-e2e.yml` 一步）；全仓重复搜索该字符串 → **0 命中**；两步本地复跑均 `exit 0`、`error_count=0` |
| 2 | **授权②全量改造（已落地）** | 全仓 `perf_ceiling_ms(` 命中 **37 行 = 定义 1 + 调用 36**，分布 **20 个测试文件**（与 (15) 登记逐条对齐，无遗漏无越界）；入口为新增 helper `tests/conftest.py` |
| 3 | **授权③`python_version` 3.10→3.12（已落地）** | **非顺手升级**：去 flag 后 mypy 唯一剩余错误为 `numpy/__init__.pyi:737: error: Type statement is only supported in Python 3.12 and greater [syntax]`（numpy 2.5.1，经 streamlit 传递进入 lint 环境）。**实测排除** `numpy.*` 的 `follow_imports="skip"` 与 `ignore_errors=True` 两种 override（解析期错误，override 管不到）；`--python-version 3.12/3.13` 均 0 error → **上抬目标是唯一不改门禁强度的解法**，已请示获准 |
| 4 | **上抬版本未把桩变回 `Any`** | 复验 `reveal_type(st.selectbox(...))` → `str`，即 streamlit 桩在新目标下**仍然生效**；否则等于换个方式重现假绿。**已接受代价**：不再为项目代码强制 3.10 级语言兼容性（`requires-python` 仍 `>= 3.10`） |
| 5 | **改造设计：控制变量必须 `operation-independent`** | 入口 `perf_ceiling_ms(b)` = `b × max(1.0, reference_workload_ms() / REFERENCE_WORKLOAD_MS_DEV)`；控制体为**确定性零依赖纯 CPU 参考负载**（4000 次 dict/format/sort 循环），**不触碰任何 DevSquad 模块**。校准机（本机 `0.982ms`）系数恒 `1.0` → **每处天花板 = 原预算，本地强度零稀释**；各站点预算数字**逐字保留**，无一处放宽 |
| 6 | **一次被证伪又纠正的设计（受控实验）** | 首轮对 dep-scan 用了**同代码路径**控制；注入 **13.7×** 恒定因子退化后实测 `op=556.4ms / control=64.4ms / ceiling=2971.4ms` → **原 200ms 门禁 FAIL（正确抓红），同路径门禁 PASS（漏检）**。同路径控制随回归同比例抬高天花板 → **构造上永不失败**。设计已**全量回退** |
| 7 | **逐类负向验证（门禁绿不算数）** | `test_quality_calibration_gate` 注入 `+6.0s` → **RED** `6.0152 not less than 5.0`；`test_dispatch_pipeline_integration::test_02_dry_run_completes_quickly` 注入 `+6.0s` → **RED** `6.1336 not less than 5.0`；mypy 新配置临时探针 `def f() -> str: return 42` → 报 `Incompatible return value type`，随后**立即删除并复验不存在** |
| 8 | **CI 等价两段实测（keys 已 unset）** | unit 段（`--cov=scripts --cov-fail-under=70`）：`7947 passed, 27 skipped, 6 deselected in 517.20s`，`Total coverage: 79.49%`，**0 failed**（所有 perf 门禁均**在 coverage 插桩下**通过，更严方向）；e2e+integration 段：`1627 passed, 4 skipped, 81 deselected in 390.33s`，**0 failed** |
| 9 | **全部门禁本地复刻** | ruff `All checks passed!`；radon 无 D+；mypy 两步 `exit 0`；`check_version_consistency.py` `73 passed/4 skipped/0 failed`；`check_module_activation.py` `8/8 production-verified, 27 self-call verified`；`check_dependency_lock.py` aligned；`check_test_quality.py --fail-on major` `0 MAJOR/373 MINOR`；`check_hidden_content.py --allow-homoglyph-in-redteam` → `OK: no hidden content found`（exit 0）。**该脚本须带命令记录**：无 flag 时报 **10 个 homoglyph finding / exit 1**（全为 `tests/security/red_team.py`×6 与 `test_output_validator_redteam.py`×4 的故意攻击样本），且 `grep .github/workflows/` **0 命中** → **它不是 CI 门禁**，仅为本地工具 |
| 10 | **一次 e2e 运行的失败结论作废（诚实更正）** | 首跑报 `2 failed, 1625 passed`（`test_v459_async_pipeline.py` 两项，`Connection error.`），曾被标为"疑似回归"。**定性错误**：`openai` SDK 把**存在但为空**的 `OPENAI_BASE_URL` 视为已配置 base_url → 无 host 请求 → `APIConnectionError`（重试 9 次）；探针证明注入的 `httpx.MockTransport` handler 全程正常被调用。改用 `env -u`（**unset 而非置空**）后该文件 `9 passed`、整段即第 8 条。**教训入档：模拟"CI 不注入 key"必须 unset** |
| 11 | **数量交叉核对** | unit `7947` + e2e/integration `1627` = `9574`；上一批等价两段 `7919` + `1631` = `9550`。本批**未新增/删除测试**，差异来自上一批失败项转绿与 skip/deselect 集合差异 → **无测试被静默吞掉**；`tests/conftest.py` 为 helper 模块，不参与收集计数 |
| 12 | **真实外部 CI / Release E2E 全绿（提交 `cac2ba7`）** | **CI `35971001606`**：9 job → 8 success + `performance` skipped（tag-only）；`test (3.10)/(3.11)/(3.12)` 均为 `7936 passed, 38 skipped, 6 deselected`；`e2e` `1627 passed, 4 skipped, 81 deselected in 76.48s` + 反幽灵 `1 passed`。**V4.5.9 Release E2E `35971053128`（`workflow_dispatch`）**：3/3 job 全绿，`V4.5.16 Final Release Gate` 17 步全 ✓ 含 `Run mypy hard check`。**Perf Baseline `35971001656`**：Mock path success / All paths skipped（tag-only）。**No Secrets `35971001575`**：success |
| 13 | **门禁可信度核验（绿的前提）** | `gh run view --log` 实证 lint job 执行的**确为去 flag 后的新命令**（两处 mypy 均无 `--no-site-packages`）→ 新 workflow 定义被真实采用。另核该运行中的 `unused section(s)` note：对照**上一运行 `35941433287` 同 note 计数 = 1** → **既有现象，非本批引入**，不作归因。**CI 的 `e2e` 数字与本地代理段逐字一致（`1627/4/81`）**，互为印证 |
| 14 | **numpy 错误的版本边界（自我更正）** | 第 3 条的语法错误触发条件是**环境装了 numpy ≥ 2.5**；实测 PEP 695 `type X =` 计数：numpy **2.4.6 = 0**、**2.5.1 = 65**；**CI 的 lint job 解析到 2.4.6**（`streamlit-1.64.0` 的 `numpy<3` + runner 缓存）→ **CI 即便保持 3.10 也不会因此变红**。故该项**不得表述为"CI 的 lint job 曾失败"**（过度归因，已就地更正 CHANGELOG 与 PRD §6 (16) 三）。上抬目标的价值是让**去 flag 后的门禁在 CI 与开发机两侧都可用** |

### 用户裁决原文（逐条引用）

1. **「Maintain the current Minor version unless explicitly approved by me. No Minor version upgrades should be implemented without prior authorization.」** → 本批 `VERSION` 维持 `4.5.20` 不动，**未升 MINOR**。
2. **「Address and resolve all 3 existing mypy errors … comprehensive fixes … rather than … temporary workarounds.」** → 三个错误已于上一批在**源码层**根因修复（无 `type: ignore`、无放宽检查、无改门禁）；本轮进一步修掉**产生盲区的门禁本身**。
3. **「Review the complete results of the CI … and Release E2E … determine whether the current build should proceed to the W1 wave deployment schedule.」** → 见下方判定。
4. （对 (15) 方案表的裁定）**选 C. 全量改造**（初选 11 处，得知真实命中数后确认为"全量"）。
5. （对 mypy 收口方式的裁定）**选 `python_version` 3.10→3.12**。
6. （第九轮遗留第①项）**批准去掉 `--no-site-packages`**。

### W1 准入判定（第十轮）

**结论：准予进入 W1 波次 —— 维持第九轮判定不变，并为其补强证据。**

| 判定要素 | 第十轮状态 |
|---|---|
| CI / Release E2E 完整结果（**真实外部**） | **无失败项**。CI `35971001606` 8 success + 1 skipped(tag-only)；Release E2E `35971053128` 3/3 全绿（含 17 步 Final Release Gate）；Perf Baseline / No Secrets 亦绿（第 12 条）。**且已核验 CI 跑的确实是去 flag 后的新命令**（第 13 条） |
| CI 等价两段（本地代理） | `7947` + `1627`，均 **0 failed**；e2e 段与真实 CI **逐字一致**（`1627/4/81`） |
| 第九轮登记的间歇性红源（(15)） | **已根治** —— 36 处绝对时序断言改为环境等比天花板，且逐类负向验证仍能抓红；不再登记为"已知 flaky" |
| 门禁可信度 | **已修复并验证** —— 去 flag 后 mypy 的检查范围不再被削掉；负向探针证明它真的在检查（第 7/13 条） |
| 新增失败项 / 新增顾虑项 | **无** |

### 诚实声明（第十轮）

1. 本轮**不是** 7-Role 评分轮次，是**门禁/判据变更的实施 + 准入复核**。**未重新召集 7 角色评分。**
2. **三项变更全部先请示后执行**，无一项属擅自改动：去 flag、全量改造、`python_version` 上抬均经用户显式授权（见"用户裁决原文"）。**改判据属质量门禁变更**这一边界被严格遵守。
3. **不把"门禁绿"当作"被检查对象正确"**：去 flag 的整个过程恰恰是这一原则的实证 —— 绿是**检查范围被 flag 削掉**后的绿。本轮所有"通过"结论均附**负向验证**（能抓红）作为前提。
4. **主动作废自己的一次失败定性**（第 10 条）：首次 e2e 的 `2 failed` 被我标为"疑似产品回归"，实为**环境模拟方法之误**（空串 vs unset）。**该结论作废、不作证据**，教训已写入 CHANGELOG 更正条与 PRD §6 (16)。
5. **未擅自扩大范围**：`(15)` 中登记的 9 处按"全量"实施，但**未顺带删除或改写任何无关判据**；先例文件 `test_scratchpad_history_store.py` **故意保留自有 helper 不迁移**（属有意为之，非遗漏）。
6. **未把推测写成结论**：`check_dependency_lock.py` 首次本地 FAILED 被定性为**本地伪影**（`mypy` 不在 PATH），并有对照证据（加入 `.venv/bin` 后 `system=2.2.0, lock=2.2.0 → OK`）；**未据此声称脚本有问题**。
7. **未擅自切 tag**：`4.5` 线内是否切 PATCH tag 属**发布动作**，**仍待裁定**，本轮不执行。**【第十一轮：已裁定并完成】** —— 用户裁定 **"本批不打，W1 后一并打"**；本轮仍未执行任何 `git tag`（见第十一轮"七"）。
8. **主动更正自己的两处过度/不完整归因**：
   - （第 14 条）我最初把 numpy 语法错误表述为"lint job 会因此失败"，但实测 **CI 用的是 numpy 2.4.6（PEP 695 计数 = 0），CI 根本不会红**。已就地更正 `CHANGELOG.md` 与 PRD §6 (16) 三，明确该错误**只在解析到 numpy ≥ 2.5 的环境复现**。**"本地能复现"不得写成"CI 曾失败"。**
   - （第 9 条）我此前记录 `check_hidden_content.py` "0 findings" **未注明命令**：无 flag 时报 **10 findings / exit 1**（全为 red-team 刻意样本），且该脚本**不在任何 CI workflow 中** —— 既不能声称"0 findings"（除非带 `--allow-homoglyph-in-redteam`），也不能把它当作 CI 门禁。**记录第三方工具结论必须带完整命令**。

### 结项与遗留（第十轮）

- **本轮结项**：① 第九轮遗留第①项（去 `--no-site-packages`）**裁定并完成**；② 全量时序断言改造（36 处 / 20 文件）完成并逐类负向验证；③ `python_version` 上抬落地；④ 门禁可信度的**负向验证**补齐；⑤ **W1 准入维持"准予进入"**（**真实外部 CI `35971001606` + Release E2E `35971053128` 在 `cac2ba7` 上全绿**，第 12/13 条）；⑥ 提交 `cac2ba7` 已推送至 `v4.5.20-patch`。
- **仍待后续裁定**：② `st.mermaid` 死分支保留还是删除；③ `4.5` 线内是否切 PATCH tag（发布动作）；④ **P2-4** 未归因；⑤ black 全仓红的成因与是否纳入门禁。**【第十一轮：五项已全部裁定并完成】** —— ②**删除**；③**本批不打、W1 后一并打**（仍未执行）；④**机制级归因闭合 + 已修**；⑤**成因定案（从未有任何格式化器被强制过）+ 采纳 `ruff format` 为唯一门禁 + 一次性全仓 reformat**。证据见 PRD §6 (17)。
- **不静默**：本轮的失败定性更正（第 10 条）与三项授权变更均已写入 `CHANGELOG.md` 的 `## [Unreleased]` 及 PRD §6 (16)。

**【第十一轮：已裁定并完成】** —— 上述第 9/10 条遗留项已由用户裁定并实施（四项遗留 + 一次显式授权的门禁变更），证据与原始输出见 PRD §6 (17)。

---

## 第十一轮：四项遗留裁定与实施 + reformat 门禁接入（2026-09-24）

### 触发

用户问「四项非阻断遗留你的建议是什么？我感觉死分支去掉，tag 不着急，P2-4 未归因要找出原因？black 全仓红的成因也要搞清楚」，并提出「然后可以进入 W1 波次」。经 `AskUserQuestion` 追问后逐条裁定（用户随后跳过了一次追问，按推荐路径 A/A 继续）。

### 用户裁决原文（逐条引用）

| # | 议题 | 用户裁决 |
|---|---|---|
| 1 | `st.mermaid` 死分支 | **删除并修正错误注释** |
| 2 | 格式化门禁 | **采纳 `ruff format` 为唯一格式化门禁 + 一次性全仓 reformat**（**显式授权门禁变更**） |
| 3 | P2-4 | **显式隔离 provider key** + **固定「键置空」为本地验收入口** |
| 4 | tag（发布动作） | **"本批不打，W1 后一并打"** |
| 5 | 波次 | 据此**授权进入 W1 波次** |

### 事实记录（含真实命令输出，命令见 PRD §6 (17)）

1. **`st.mermaid` 死分支已删除**。死的是 `callable(getattr(st, "mermaid", None))` 那一半：**全仓无 streamlit pin**（`requirements.txt`/`pyproject.toml` 均 `>=1.28.0`，`requirements.lock` 中该行为注释态），本机 1.59.0 与 CI 1.64.0 实测 `hasattr(st, "mermaid") == False`。同时更正**三处错误陈述**（凭空写下的 "pinned Streamlit **1.57.0**" 与两处 docstring）。**零测试影响**（相关测试只断言 `viz.to_mermaid()` 的字符串）。
2. **P2-4 归因闭合（机制级）**：未隔离 provider 环境 → `MultiAgentDispatcher(autonomous_enabled=True)` 自建 `auto` 链 → `loop_controller._llm_role_votes` 对 **5 角色串行**真实调用 → Moka key 已失效（401，见 (12)）→ `MOKA_MAX_RETRIES=3` × `DEFAULT_BACKOFF_BASE=2` 退避 → 单次 `auto.generate()` 实测 **10.68s** → 累积突破 `--timeout=60`。**性质 = 测试未隔离环境输入，不是产品回归。** 修复 = `tests/conftest.py::isolated_provider_env()`；**负向对照证明"只删环境变量"无效**（`create_backend` 首次调用会自行 `_load_dotenv()` 读 `.env`，须同时 patch 该函数）。
3. **black 全仓红定案：不是 black 的问题，是"从未被格式化过"**。宽度扫描 8 档中 `line-length=120` 已是**全宽度最小值（453）但仍非 0**；同配置下 `ruff format --check` 报 430/435，两个格式化器**一致判红**；`black 25.11.0` 同样判红 → **否证版本漂移**。接线核查：`ruff check` 是 linter 且**显式忽略行宽**；`ruff format` 仅在 `.pre-commit-config.yaml` 中声明；`black==26.5.1` 在 dev 依赖里但**未接任何 CI job、未接任何 hook**。
4. **一次性全仓 reformat 实施**：`435 files reformatted, 268 left unchanged` → `703 files already formatted`。**打红了两个阻断级门禁，两处均已修复**：
   - `check_dispatcher_size.py`：7 文件超基线（`deterministic_rule_engine.py` +114 最多），机制是 `f(g(...))` 拆行**每处净增 2 行**；处置用门禁**自带的** `--write-baseline` 刷新，**门禁逻辑/阈值/比较方式未改**。**代价如实登记**：2 个文件因这次 reformat 首次越过 800（`dependency_hallucination_checker.py` 740→805、`worker.py` 791→837）。**顺带发现旧基线本身早已 stale**（161 文件数值不符、缺 6 个键，只阻断增长不阻断缩减所致）。
   - `check_test_quality.py`：`0 MAJOR / 373 MINOR` → `1 MAJOR / 371 MINOR`；红点是 **docstring 散文**里的 `@lru_cache` 被行级正则命中，而豁免靠**位置型窗口** `range(0, 8)`，reformat 插入 1 空行把真 fixture 的 noqa 推出窗口。**处置 = 不动门禁**（不动窗口/pattern/severity），让散文与注释不再出现该字面量；**拒绝把窗口 8→9**（那是放宽判据）。
5. **「CI 无 key」收窄**：作为全称陈述**不成立**（`test.yml::e2e` 与 `release-e2e.yml::real-llm-e2e` 是 keyful 的）；收窄为"**在运行这两个测试文件的每一个 CI job 上成立**"（`test` 矩阵与 `final-gate` 均无凭据块）。
6. **本地验收入口固定化**：新增 `scripts/acceptance_local.sh`（凭据**置空**而非 unset，理由：根 `conftest.py::_load_env_file()` 会把缺失键再从 `.env` 注入），脚本自带凭据长度自检输出。
7. **门禁接入（本轮唯一一次门禁变更，已获显式授权）**：`test.yml::lint` 与 `release-e2e.yml::final-gate` 各加一步 `ruff format --check .`，**同一检查范围**；**负向验证**：`GREEN_EXIT=0` / 注入未格式化探针 `RED_EXIT=1` / 删除探针 `RESTORED_EXIT=0`。
8. **本轮门禁实跑**（逐条，非引用旧值）：`ruff check . --ignore=E501 --no-cache` → `All checks passed!`；`ruff format --check .` → `703 files already formatted`；`check_test_quality.py --fail-on major` → `TQ_EXIT=0`；`check_dispatcher_size.py` → `SIZE_EXIT=0`；radon / mypy ×2 / bandit（CI 档）/ version / anti-ghost / hidden content / test pyramid / config consistency 全绿。

### 诚实声明（第十一轮）

1. 本轮**不是** 7-Role 评分轮次，是**四项遗留的裁定实施 + 一次显式授权的门禁变更 + 验收入口固定化**。**未重新召集 7 角色评分。**
2. **门禁变更只有一处且已获显式授权**：`ruff format --check` 接入两个 workflow。**未擅自改动任何既有判据的严格度** —— 豁免窗口、pattern、severity、`--max-lines`、基线比较逻辑一律未动。
3. **主动作废自己本轮的一处错误结论**：我一度断言 reformat「引入了 7 条无效 noqa 警告」。受控对照（worktree `/tmp/ds_prereformat`，`--no-cache` 冷跑）证明**这 8 条警告 reformat 之前就存在**，此前未现身只因 **ruff 缓存**命中未变更文件。**该定性作废**，已在 PRD §6 (17) 四诚实记录。
4. **两次自我更正（含一次修复失败）**：（a）ARG002 归因错误已更正（该错误**确由 reformat 引入**，经 worktree 冷跑 `All checks passed!` 对照确认）；（b）`check_test_quality.py` 的**第一次修复失败** —— 只改红点所在行后，失败点**从 269 行移到 271 行**（我新增的说明段把 noqa 推得更远），最终改为"散文一律不写该字面量"。
5. **代价不隐瞒**：size 基线刷新即把 7 个文件的逐文件上限抬高（最多 +114），其中 2 个首次越 800，已登记为待拆分债务（§9 D10）；`check_test_quality.py` 的 MINOR 数 373→371 的**两条差额已逐条定量**（−1 真实丢失、−1 额外抑制），不作噪声。
6. **三项"看起来红"的逐条给对照证据**，既**不当成本轮问题**也**不当成通过**：`check_constructor_params.py`（reformat 前同为 exit 1/11 个，且不在任何 CI workflow）、`check_bandit.py` 默认档（前后同为 `0 HIGH + 7 MEDIUM`，CI 走 `--max-medium 7`）、`check_dependency_lock.py`（本地 PATH 伪影）。
7. **不把"门禁绿"当作"被检查对象正确"**：新接入的格式化门禁**做了负向验证**（三个退出码），P2-4 的隔离做了**负向对照**（证明"只删变量"是无效隔离）。
8. **发布动作一件未做**：**未执行任何 `git tag`**，`VERSION` 仍为 `4.5.20`（tag 按用户裁定"本批不打、W1 后一并打"）。

### 结项与遗留（第十一轮）

- **本轮结项**：① `st.mermaid` 死分支删除 + 三处错误陈述更正；② P2-4 机制级归因闭合 + 修复 + 负向对照；③ black 全仓红成因定案；④ 一次性全仓 reformat + 两处阻断级门禁回归的定位与处置；⑤ `ruff format` 门禁接入（获授权）+ 负向验证；⑥ 本地验收入口脚本化并实跑；⑦ PRD §6 (17)、本记录第十一轮、`V4.6.1_cleanup_PRD.md` 条目作废回写。
- **登记债务（不静默）**：**D9** 三处"判据与源码格式耦合"的残留脆弱性（size gate 非空行数指标 / `_is_noqa_suppressed` 位置硬编码 `range(0, 8)` / 行级正则不区分代码与散文）；**D10** 两个文件因 reformat 首次越过 800 LOC；**8 条存量 `invalid noqa` warning**（`# noqa: test-quality` 是 `test_quality_guard` 的自定义标记，非 ruff 代码；不阻塞，未擅自改名）。
- **仍待执行（发布动作）**：tag 由用户裁定 **W1 后一并打**。

---

## 第十二轮：W1-0 契约先行交付 + 门禁自身缺口被负向验证抓出并加固（2026-09-24）

### 触发

第十一轮结项后，用户以「然后可以进入 W1 波次」明确授权进入 W1。按 W1 表，`W1-0`（E7 契约先行）为 W1-1~W1-7 的规格前置项，故本轮只做 W1-0，不越界开工 W1-1。

### 本轮性质（不夸大）

**不是 7-Role 评分轮次**，是 **W1-0 的执行 + 一次在自建门禁内部发现并加固的缺陷**。未重新召集 7 角色评分。

### 交付物（3 项，均已在工作树）

| # | 文件 | 内容 |
|---|---|---|
| 1 | `docs/reference/DETERMINISTIC_CONTRACT.md`（新建） | **18 条契约条款（C1–C18）**，每条含条款 id / 规则正文 / `Effective`（生效版本或波次）/ `Proven by`（真正的证据测试）。按 R9 分层：SKILL.md 只留"调用方必须知道"的承诺，完整门语义表 + 规则优先级细节留在 reference |
| 2 | `SKILL.md`（+35 行，394→429） | 新增 `## 📜 Deterministic-Layer Contract (V4.5.20 W1-0)`：3 条承诺（零 API Key / 覆盖不全非零退出 / `--preview` 零 LLM 调用）+ 契约文档指针 + **状态诚实标注**（本节是写在实现之前的规格，`Effective` 才是"今天什么已生效"的权威） |
| 3 | `scripts/check_skill_contract.py` + `tests/unit/test_check_skill_contract.py` | 反漂移门禁 + **23 项测试**；独立于 `check_version_consistency.py`（E7 要求职责分离：版本门禁管版本号，本门禁管契约，契约失败不得读作发布版本失败） |

### 生效波次分布（R7：文档承诺不得超前于代码）

**已生效 2 条**（C14 `delegate.zero_api_key` / C15 `delegate.strict_marker_fail_closed`，随 V4.5.10 交付）；
**待生效 16 条** —— W1-1：C4–C7；W1-2：C8；W1-3：C1–C3；W1-4：C11–C13；W1-5：C18；W1-6：C16–C17；W2-1：C10；W2-2：C9。
每一条的波次标注**必须能被门禁反查**（契约里的波次 token 必须在 PRD 中以**独立 token** 存在，防"标注悬设"与 `W1-1`/`W1-10` 前缀碰撞）。

### 事实记录（含真实命令输出）

1. **门禁正向实跑**：`python scripts/check_skill_contract.py` → `skill-contract: 18 clauses, 4 SKILL.md commitments` / `PASS: all clauses intact; future waves resolved: W1-1, W1-2, W1-3, W1-4, W1-5, W1-6, W2-1, W2-2` / `NOTE: presence only — this is a drift alarm, not proof of behaviour.` / **`EXIT=0`**；`--quiet` **完全静默**（`EXIT=0`）。
2. **门禁自身的缺口——本轮最值得留痕的一条**。首跑负向 **N2**（把 C1 索引行的 `Effective` 单元格置空）返回 **`EXIT=0`**：门禁**漏掉了它本应防住的那类失效**。机制 = 原断言是**全文档子串检索** `clause.effective not in contract_text`，而同表 C2/C3 两行仍带 `W1-3`，故单格被置空后**整体仍是绿的**。性质与"门禁绿 ≠ 被检查对象正确"同族：**断言强度弱于它宣称要防的失效**。
   **加固（收紧，非放宽）**：改为读**该条款自己的索引行第 4 列**并做等值比较（`_declared_effective()`）；补 **N6**（删整条索引行 → `has no row in the clause index`）与两条单测（`test_09` 改行内断言、新增 `test_09b`）。加固后 **N2 → `EXIT=1`**，报 `[gate.secret_exclude_priority] clause index row declares Effective ''`。
   **依据**：本文《预防措施》#13 —— "任何门禁上线前，必须显式构造**该门禁本应防住的那次真实失效**并确认门禁变红"。首跑绿不等于门禁可用。
3. **6 条负向验证（均在真实文档的临时副本上做受控篡改，仓库零改动）**：N1 契约 `unsupported_ext`→`unsupported_type` → **1**；N2 置空单格 → **1**（加固后）；N3 SKILL.md 删 `non-zero` → **1**；N4 删契约文档 → **2**（区分"漂移"与"读不到"）；N5 **只在 PRD** 把 `W1-3`→`W1-9` → 3 条 `dangling annotation` / **1**；N6 删整条索引行 → **1**。对照：未篡改副本 **0**。
4. **测试**：`tests/unit/test_check_skill_contract.py` → **`23 passed`**。**用真实文档的副本、逐条只删一处** —— 手写迷你文档只能证明"比较逻辑对一份为通过而写的文档有效"。两个首版测试失败均为**测试 bug 而非门禁 bug**，且**未为让它们通过而改门禁**：（a）替换 `` `secret_exclude` `` 仍通过，因为 `secret_exclude` 作为子串存活于条款 id `gate.secret_exclude_priority`；（b）断言了一条 docstring 里并不存在的短语。
5. **门禁接线（属门禁变更，显式登记，只增不减）**：`test.yml::lint` 新增 `Skill contract drift check (V4.5.20 W1-0)`；`release-e2e.yml::final-gate` 新增同名 hard step（两者范围一致，防"发布接受了 CI 拒绝的契约"）。**未删除/放宽任何既有断言、未改任何 flag、未缩窄任何门禁范围**。授权依据 = E7「必须门禁化，否则文档必漂」+ W1 表 W1-0 行。
6. **本轮门禁实跑**（逐条实测，非引用旧值）：契约门禁 `EXIT=0`；单测 `23 passed`；`check_test_quality.py --fail-on major` → `OK: no issues at or above MAJOR severity`（`TQ_EXIT=0`）；`check_dispatcher_size.py` → `PASS`（`SIZE_EXIT=0`）；`mypy scripts/ skills/` → `errors=0`；`radon cc scripts/check_skill_contract.py -nd -s` → 0 行；`ruff format --check` → 全仓 `already formatted`；`ruff check` → `All checks passed!`；`check_version_consistency.py --strict` → **`73 passed, 4 skipped, 0 failed`**（`EXIT=0`）。
7. **附带修复**：编辑 `SKILL.md` 触发 `check_version_consistency.py` 的 **TRAE 缓存内容比对**（L1 `~/.trae-cn` / L2 `~/.trae` / L3 workspace `.trae` 三处副本落后：`source=429L, cache=394L, first diff at line 148`）→ 同步三处后 `--strict` 转绿。**任何 SKILL.md 改动都会触发**，已定为常规步骤。

### 诚实声明（第十二轮）

1. **本门禁不能证明契约被实现，只防漂移**。该边界已写在**脚本 docstring、契约文档 §2、SKILL.md 状态标注**三处，避免被当作"功能已可用"的证据。
2. **契约中 16 条尚未实现**（见上"生效波次分布"）：窗口期内 SKILL.md 的承诺**强于**代码能力 —— 这是 R7 明确要求的先写顺序，不是疏漏。
3. **`Proven by` 指向的测试大多尚不存在**（`tests/test_review_preview.py`、`tests/test_rule_engine.py`、`tests/test_coverage_exit_codes.py` 等），它们是 W1-1…W1-5 / W2 的交付物。此处是"**约定证据位置**"，**不是"证据已就位"**。此条须与第 1 条同读。
4. **门禁在首跑时并不能兑现其被写下的承诺**（N2 首跑 `EXIT=0`，见事实记录 2）。已加固，但**该事件本身不得被"加固后转绿"抹去** —— 它正是"必须做负向验证、不能只看首跑绿"的实例。
5. **关键词是子串匹配**，可能被"关键词仍在、语义已改写"绕过（把否定句改成反问句）。这是本门禁的已知上限。
6. **契约字面量（8 个门名 / 四层链层名 / `rules check` 命令名）是实现期的接口约束**：实现若必须偏离，须**先改契约再改代码**，不得实现完再补。
7. **发布动作一件未做**：**未执行任何 `git tag`**，`VERSION` 仍为 `4.5.20`（tag 按用户裁定「本批不打、W1 后一并打」）。

### 结项与遗留（第十二轮）

- **本轮结项**：① 契约文档 18 条落地（含 `Effective` / `Proven by`）；② SKILL.md 三承诺章节 + 状态诚实标注；③ 反漂移门禁 + 23 测试；④ 门禁接线（两个 workflow，只增不减）；⑤ **门禁自身缺口由 N2 首跑暴露并按《预防措施》#13 加固**；⑥ PRD §6 (18) / CHANGELOG / 本记录回写。
- **遗留**：W1-1…W1-7 按 W1 表推进（下一项 W1-1）；W1 发布前须在真实仓库跑完整路径 E2E（过滤 → 规则匹配 → 审查 → 报告）；tag 缓办。

---

## 第十三轮：W1-1 E2 规则解析层交付（2026-09-24）

### 触发

第十二轮结项后，用户以「然后可以进入 W1 波次」授权 W1 全波次，本轮按 W1 表推进至 **W1-1**（E2 规则解析层），即 W1-0 之后的第一个实现项。

### 本轮性质（不夸大）

**不是 7-Role 评分轮次**，是 **W1-1 的执行 + 一次"契约先行"引发的门禁咬合验证**。未重新召集 7 角色评分。

### 交付物（4 项，均已在工作树）

| # | 文件 | 内容 |
|---|---|---|
| 1 | `scripts/collaboration/rule_engine.py`（新建，**190 行 / 156 非空行**） | 四层链解析器，实现契约 **C4–C7** |
| 2 | `scripts/collaboration/system_rules.json`（新建） | 系统层（C6），含末条 catch-all `**` |
| 3 | `pyproject.toml` `[tool.setuptools.package-data]` | 新增 `"scripts.collaboration" = ["*.json"]`，使 JSON 随包分发 |
| 4 | `tests/test_rule_engine.py`（新建，**26 项 + 16 subTest**） | C4–C7 的 `Proven by` 证据 |

### 实现要点（逐条对齐契约，非事后补文档）

- **C4 四层链**：`--rule <path>` → `<repo>/.devsquad/rule.json` → `~/.devsquad/rule.json` → 随包 `system_rules.json`。**层缺失 = 静默跳过**（`load_rules(None)` / 文件不存在 → `()`）；**层存在但不可解析 = `RuleConfigError`**（缺 `rules` list / JSON 非法 / `pattern` 非字符串 / `body` 非对象）。二者语义不同，不可混同。
- **C5 第一个匹配生效**：无 merge、无 override；**层内以文件顺序**决定，先匹配者胜。
- **C6 系统层永远存在**：三条规则，末条 `**` catch-all；`resolve()` 在无匹配时**抛错并指明 origin**，使"无规则的未定义状态"不可能出现。
- **C7 安全豁免硬编码**：`security_resolution()` **先于**所有可配置层执行，路径模式集硬编码在模块内（**19 条**，与 PRD §6 (19) 一致），**不读任何文件**。**刻意不复用** `permission_guard._SENSITIVE_PATH_PATTERNS`（那是路径穿越/注入防护，语义不同且耦合深）。
- **方言扩展**：`PurePosixPath(path).match(pattern)` 为**右锚定**；实测 `PurePosixPath("cli.py").match("**/*.py")` = **`False`**（`fnmatch.fnmatchcase` 同结论），即 `**/` **不**匹配零级目录。**这是真脚枪而非口味问题** —— 无此扩展时随包系统层会漏判所有顶层 Python 文件。故增加"`pattern.startswith("**/")` 时用 `pattern[3:]` 再匹配一次"，并**写入契约 C4**（先改契约再改代码）。

### 契约的两处修订（先改契约，后改代码）

| 条款 | 修订 | 触发原因 |
|---|---|---|
| C4 | 新增「层存在但不可解析 = `RuleConfigError`，仅*缺失*静默」+「右锚定 + 前置 `**/` 可匹配零级目录」 | 实现暴露了两处原契约未定义的语义 |
| C5 | 新增「层内**文件顺序**决定，先匹配者胜」 | 原契约只定义了跨层序，未定义层内序 |

对应 `check_skill_contract.py` 的 C4/C5 关键词行随之补充（`--rule <path>` / `system_rules.json` / `RuleConfigError` / `right-anchored` / `file order`）。**未放宽任何门禁断言**。

### 事实记录（含真实命令输出）

1. **门禁咬合验证 —— 本轮最值得留痕的一条**：改完契约首跑契约门禁即**变红**：`FAIL: [rule.first_match_wins] keyword 'file order' is missing from docs/reference/DETERMINISTIC_CONTRACT.md`。原因 = 我原文写的是 "rule order in the file"，而门禁按字面量 `file order` 检查。**处置 = 改文档措辞，不改关键词表**。这条红正是"W1-0 门禁是承重墙而非装饰"的证据。
2. **单测**：`tests/test_rule_engine.py` → **`26 passed, 16 subtests passed`**。六个类：层序（含 4 层全序 + 缺层不抛）/ 无 merge（逐字携带 / 特异性不敌层序 / 层内文件顺序 / 嵌套 body 原样）/ 安全墙（CLI `include` 拉不回 `.env`、项目层关不掉 `**/*.pem`、用户层认领不了 `.ssh/id_ed25519`）/ 配置错误 6 例全抛 `RuleConfigError` / glob 方言（零级目录、右锚定、不越界、**随包** JSON 末条为 catch-all）/ `default_sources()` 默认路径与契约一致。
3. **5 条受控突变 + 1 条对照（在 `/tmp` 的整包副本上做，仓库零改动）** —— 依据本文《预防措施》#13：

   | 突变 | 内容 | 结果 |
   |---|---|---|
   | 对照 | 未突变副本 | `exit=0  26 passed, 16 subtests passed` |
   | N1 | **fail-open**（安全墙改为"仅当无配置层认领时才生效"） | `exit=1  4 failed` |
   | N2 | **merge**（生效 body 被并上额外字段） | `exit=1  6 failed` |
   | N3 | **丢兜底**（删 `system_rules.json` 的 `**`） | `exit=1  1 failed` |
   | N4 | **缺层报错**（把静默跳过改成抛异常） | `exit=1  9 failed` |
   | N5 | **砍方言**（去掉 `**/` 零级目录扩展） | `exit=1  7 failed, 10 subtests passed` |

   `negative pass complete; all mutations caught`。**副本必须整包复制**：`scripts/collaboration/__init__.py` 会 import 同包兄弟模块（`._version`、`.adaptive_role_selector` 等），只拷两个文件会得到 collection error 而非结果（首跑即为该错）。
4. **本轮门禁实跑**（逐条实测，非引用旧值）：`ruff check . --ignore=E501 --no-cache` → `All checks passed!`；`ruff format --check .` → exit 0；`check_skill_contract.py` → `PASS: all clauses intact; future waves resolved: W1-1, W1-2, W1-3, W1-4, W1-5, W1-6, W2-1, W2-2`；`tests/unit/test_check_skill_contract.py` → `23 passed`；`radon cc scripts/collaboration/rule_engine.py -nd -s` → 0 行；`mypy scripts/ skills/ --ignore-missing-imports --no-error-summary` → 0 errors；`check_test_quality.py --fail-on major` → `OK: no issues at or above MAJOR severity`；`check_module_activation.py` / `check_dependency_lock.py` / `check_dependency_sync.py` / `check_config_consistency.py` / `todo_drift_monitor.py --format text` / `check_dispatcher_size.py` / `check_version_consistency.py --strict` → 全部 exit 0；`check_hidden_content.py --allow-homoglyph-in-redteam` → `OK`。
5. **门禁接线**：**本项零新增门禁、零门禁变更**（契约门禁已于 W1-0 接入两个 workflow 且范围一致）。W1-1 不构成任何门禁变更，故不触发"须先请示"。
6. **PRD 回写**：§6 (18) 的「15 条尚未实现」更正为 **16 条**（按其自身口径 18 − C14/C15 两条已 shipped）；新增 **§6 (19)** 记录本项（授权来源 / 交付物 / 契约修订 / 实现要点对齐 / 真实命令输出 / 5 突变 + 对照 / 测试 / 门禁接线 / 附带更正 / 诚实声明 / 遗留）；W1 表 W1-1 行标注**已完成**。
7. **附带更正**：`CHANGELOG.md` 同处 off-by-one（「15 of the 18 clauses are not shipped yet」）同步改为 **16**，并把时态改为"W1-0 时"以免与新状态冲突。

### 诚实声明（第十三轮）

1. **`rule_engine.py` 目前没有生产调用方** —— W1-1 交付的是"可被调用的解析层"，首个真实调用方是 **W1-2 的 `devsquad rules check`**。在 W1-2 落地前，本模块只被单测与契约证明，**不得记作"已接入管线"**（这正是 F4 教训：模块存在 + 模块单测通过 ≠ 已接线）。本项**不声称**端到端可用。
2. **契约 C4/C5 的两处修订是"实现期发现契约不足"，不是"为迁就代码改文档"**：两处都先写进契约、跑门禁确认、再写代码；且修订方向是**收紧语义定义**（补上"不可解析即报错""层内文件顺序"），不是放宽。
3. **glob 方言扩展是 `pathlib` 的实测行为，不是本项目的口味选择**；其必要性由探针（`PurePosixPath("cli.py").match("**/*.py") → False`）而非推理得出。
4. **`SECURITY_PATH_PATTERNS` 是 19 条**（PRD §6 (19)、契约、模块三处必须一致 —— 首稿我写成 20，回读源码后更正为 19），且 `T3 test_12` 断言的是「**安全层赢了**」而非「具体哪条 glob 赢」—— 因为 `**/id_ed25519` 在常量集中位置靠前且能命中 `.ssh/id_ed25519`（零级目录扩展下裸名右锚定命中），**断言具体 pattern 会把一个实现细节变成脆弱断言**；C7 关心的是"用户层没赢"。这是**测试断言被我改正**，不是**代码被测试迁就**。
5. **`tests/test_rule_engine.py` 位于根级 `tests/`**，按本项目约定归类为 **unit** 层（`check_test_pyramid.py` 的既有口径）。
6. **tag 仍未打，`VERSION` 仍为 `4.5.20`**（按第十一轮裁定「本批不打、W1 后一并打」）。**未升 MINOR**。

### 结项与遗留（第十三轮）

- **本轮结项**：① `rule_engine.py` + `system_rules.json` + package-data；② 契约 C4/C5 两处**先行**修订并经门禁咬合验证；③ 26 项测试 + 16 subTest；④ **5 条受控突变全部被抓住**（含对照）；⑤ PRD §6 (18) 更正 + §6 (19) 新增 + W1 表标注；⑥ CHANGELOG `### Added`/`### Changed`/`### Tests` 回写 + off-by-one 更正；⑦ 门禁电池全绿（零门禁变更）。
- **遗留**：W1-2（`rules check` 解释命令，C8）—— 它是 `resolve()` 的**首个生产调用方**，也是"本模块是否真能接线"的验证点；其后 W1-3 → W1-4 → W1-5 → W1-6 → W1-7；W1 发布前须在真实仓库跑完整路径 E2E（过滤 → 规则匹配 → 审查 → 报告）。

---

## 未达成共识 / 留待下轮

- **F3 分类**（Architect 主张降为"可进化"）——非阻塞，**已在 PRD §3 F3 段首标注「分类边界情形」**，本 PRD 维持"要改"分类；该分歧不影响交付内容
- **E1/E2 是否合并为一项**（Coder 主张合并）——非阻塞，取决于 R1 后新模块的边界划分；本 PRD 仍按 E2 → E1 的顺序分列（E2 为 E1 前置）
- **D1 版本槽位【已决（第三轮，用户决议）】** ——**统一固定在 4.5.20，设计阶段不预分配版本号**；原"W1–W3 用 4.6.2"的提法作废。剩余子问题（三份 WIP PRD 文件名版本号）不阻塞本 PRD
- **E9/E10 的 P2 语义【R9 已澄清】**——P2 = 论证优先级，**不构成交付承诺**；W3 = 条件波次
- **版本位归属【第八轮新增，第九轮已裁定结项】**——第八轮记为"未定"；**第九轮裁定：不得升 MINOR，维持 `4.5` 线（任何 MINOR 升级须用户显式批准）**。`VERSION` 维持 `4.5.20`；`4.5` 线内是否切 PATCH tag 属发布动作，**仍未裁定** **【第十一轮：已裁定 —— 用户决定"本批不打，W1 后一并打"；本轮仍未执行任何 `git tag`】**
- **P2-4【第八轮仍未归因，第九轮仍待】**——本机带真实 key 时全量段 2 项走真实 LLM 路径（隔离跑通过、并发撞 `--timeout=60`/drift）；本轮观测到一次长推理 `generate()` 耗时 **45.97s**（`DEFAULT_LLM_TIMEOUT_SECONDS=120.0`），与 P2-4 现象**可能相关但未做归因**，记为**未验证** **【第十一轮：已归因并修复 —— 机制链见第十一轮"2"；性质 = 测试未隔离环境输入，非产品回归。本行"未做归因/未验证"的表述已过期】**
- **CI mypy 门禁盲区【第九轮新增，第十轮已决并完成】**——`--no-site-packages` 掩盖了 3 个真实类型错误（见第九轮第 7 条）；**第十轮获用户授权，flag 已从 3 处 mypy 步骤全部移除**，并附负向探针证明门禁仍真的在检查（见第十轮第 1/7 条）。**此项不再是待裁定项。**
- **单次绝对时序断言（系统性）【第九轮新增，第十轮已根治】**——(15) 登记的"至少还有 9 处"经全量搜索实为 **36 处 / 20 文件**，第十轮获授权**全量改造**为环境等比天花板（`perf_ceiling_ms`），并逐类负向验证仍能抓红。**此项不再是"已知 flaky"。**
- **`st.mermaid` 死分支【第九轮新增，待裁定】**——pinned Streamlit 1.57.0 无此属性，该分支恒走回退（见第九轮第 8 条）；保留防御性探测还是删除分支，留待裁定 **【第十一轮：已裁定 —— 用户决定"删除并修正错误注释"，已删除；且"pinned Streamlit 1.57.0"本身是错误陈述（全仓无 pin），见第十一轮"1"】**
- **black 全仓红【第九轮新增，成因未验证】**——`450 files would be reformatted`；**只断言与本批无关**（HEAD 与本批同结论），成因未验证，是否纳入门禁待裁定 **【第十一轮：已定案 —— 成因 = "从未有任何格式化器被强制过"（非 black 缺陷、非版本漂移）；用户裁定"采纳 `ruff format` 为唯一格式化门禁 + 一次性全仓 reformat"，门禁已接入两个 workflow，见第十一轮"3"/"7"】**

### 第二轮遗留（见 PRD §9）

- **D7（最高优先级未知项）** 反幽灵门禁是否**自证**：若是，影响面可能**远超 FileBundler**，"反幽灵"这一核心承诺的有效性需重新评估 → 列为 7-Role 复核的触发条件之一 **【R11：已关闭 —— 盲区确认成立且为门禁设计缺陷；区分机制已交付并双向验证；风险等级"高"→"中"；集合外 27 个计数器的逐项接入列为独立工作项】**
- **D6** E9/E10 立项论证结论（W3-0 产出）——未通过则不实施
- **D5 执行层细节【R10：已定档并已决】** —— F4 接线位置 = `PreDispatchPipeline.prepare_execution()`；**复用**既有 `>5 文件` 硬守卫（不新造阈值）；输入仅经 `changeset` 参数。取证结论见上「第四轮」。**W0-3 可开工。**
- **PM 的过度设计张力未消解**：用户决议纳入 E8/E9/E10，PM 原警告仍成立；当前靠"边界约束 + 立项论证门槛"缓解，**不视作已达成 7 角色共识**

---

## 预防措施

1. **文档先行**：R1–R6 修订必须先落 PRD 文本，再动代码（遵循项目既定顺序：讨论 → 改文档 → 确认 → 改代码）
2. **W0 独立验证**：W0 完成后立即跑门禁负向测试（篡改一个 manifest 验证变红），确认门禁真实有效后再进 CI
3. **E2E 前置**：W1 发布前必须在真实仓库跑完整路径（过滤 → 规则匹配 → 审查 → 报告），模拟真实用户使用
4. **基线优先**：E4 的 positioning 是**新建能力**，实施前必须先测并落档**当前基线数值**（并说明测量方法），否则该 Wave 不可验收；reflection 侧复用 `AnchorChecker` 不得重写
5. **不做无限借鉴**：第 5 节"明确不改"清单为硬边界，任何新增借鉴项须先证明其不违反该清单
6. **先核实再接线（F4 教训）**：任何声称"已接入某管线"的能力，验收必须以**生产调用链**为准（grep 调用方 + 集成测试走管线），不得以"模块存在 + 模块单测通过"为满足
7. **测试名必须与执行路径一致（F4 教训）**：e2e 测试若名为"activates in XX mode"，其内部必须真的经过 XX mode；直接 import 被测模块的测试不得命名/描述为端到端激活 —— **【R11：已门禁化】** 由 `check_module_activation.py::find_unbacked_activation_claims()` 扫描 `tests/e2e/**` 并 fail-closed（PRD §6 W0-6）
8. **文档契约先于代码（E7 决策）**：W1-0 契约未写定前不启动 W1-1~W1-7 的实现，避免"实现完再补文档"导致二次漂移
9. **证据必须可复现（第三轮教训）**：写入 PRD 的任何 grep / 统计结论必须附**原始命令与输出**并标注取证时间。本轮 F1 的"8 处/10 行"错误证明：只读取证若无复核，会带病进入实施基线 —— **多角色评分不能替代证据复核**
10. **前置项未闭合不得开工（第三轮）**：W0 启动前 D5 必须先定；W2-2 启动前 D4 必须先定；W3 启动前 W3-0 必须先过；D7 若确认自证盲区须在 W0 内闭环
11. **文档承诺不得超前于代码（第三轮，E7 风险）**：契约中"尚未实现"的条款须在 SKILL.md 标注生效版本 —— 否则重演 F4 的"文档有 ≠ 功能可用"
12. **判据必须先于核实定义，且判据本身要能证伪（R11，D7 教训）**：D7 的判据（"门禁运行前读计数应为 0"）本身是**方向正确但不够强**的判据 —— 它只能发现"计数器根本不存在"，无法区分"由生产触发"与"由门禁自触发"。**判据设计时必须反问"该判据能否被被测对象自证满足"**；核实手段优先选**跳过被测对象的分离实验**（如 D7 中"跳过门禁、只跑 dispatch 读 delta"），而非在被测对象内部增加断言
13. **门禁断言必须能复现其要防的失效（R11）**：任何门禁上线前，必须显式构造"**该门禁本应防住的那次真实失效**"并确认门禁变红（D7 中即"强制 `changeset=None` 复现 F4"）。只有负向篡改测试（伪造值）不够 —— 伪造值测试证明门禁**读得懂**，复现真实失效才证明门禁**抓得住**
14. **推翻既有裁决必须写明"原裁决基于何种不完整证据"（第八轮，P2-2 教训）**：P2-2 的 report-only 裁决在**未实测"推理 token 能否吃光预算"、未在真实链路复现空内容路径**的情况下作出；本轮先取证（`MT=2048 → reasoning_tokens=2048, content_len=0`）后才改判。**任何改判都要同时留下被推翻裁决的原文与"证据缺口"**（不得只贴新结论，也不得留在同一文档里自相矛盾）；改判实现须防"假绿"（本例：抛错不进重试路径 + 成功计数移到检查之后）
15. **BREAKING 变更不得在文档里追溯式粉饰（第八轮，链顺序教训）**：当用户前后表述自相矛盾（"Moka不再用" vs "Moka 优先"）时，**必须复问并以最终裁决为准**，且在 PRD/共识记录中**如实记录反转过程**，不得只写结论

---

> **基线**: V4.5.19 (commit ae0ea8e)
> **评估对象**: `V4.5.20_ocr-learnings_PRD.md`（原 `V4.6.2_ocr-learnings_PRD.md`）
> **共识**: 第一轮 8.1/10 → **第三轮终评 8.0/10** — 有条件通过（附 R1–R9）；**第四轮（R10）为 D5 定档取证，非评分轮**
> **版本纪律**: ✅ 已按用户决议固定为 **4.5.20**，设计阶段不预分配版本号（原 4.6.2 提法作废）；**R10：W0 判定由 PATCH 改为 MINOR**
> **修订状态**: ✅ R1–R6 已回写 PRD；✅ 第二轮（F4 + E7~E10 + 用户 4 项决议）已回写；✅ 第三轮（版本固定 + F1 证据修正 11 处 + R7–R9）已回写；✅ **第四轮 R10 已回写（D5 定档 + W0 升级 MINOR）**；✅ **第五轮 R11 已回写（W0-1~W0-4 执行证据 + D7 闭环）**；✅ **第六轮已回写（W0 发布：版本升级 4.5.19→4.5.20 + release notes + 6 份文档修正 + 门禁/回归真实输出）**；✅ **第七轮已回写（发布后缺陷登记：P0-1 anyio CVE + P1-1 flaky 判据 + 连带 6 项门禁真空 + P2-1 环境隔离 + P2-2 告警日志 + P2-3/P2-4 待裁定）**；✅ **第八轮已回写（auto 链顺序反转 + 推理预算契约收紧推翻 report-only + P0-2 size gate 拆文件 + Moka 401 已知并接受；PRD §6 (10)–(12)）**；✅ **第九轮已回写（版本纪律裁定"不得升 MINOR" + 3 个 mypy 存量错误根因修复 + mypy 门禁 `--no-site-packages` 假绿盲区 + W1 准入判定；PRD §6 (13)–(14)）**；✅ **第十轮已回写（mypy 门禁假绿盲区收口 + 全量时序断言改造 36 处/20 文件 + W1 准入维持"准予进入"；PRD §6 (16)）**；✅ **第十一轮已回写（四项遗留裁定实施：`st.mermaid` 删除 / P2-4 归因并修复 / black 成因定案 + `ruff format` 门禁接入 + 全仓 reformat / tag 缓办；PRD §6 (17) + §9 D8–D10）**；✅ **第十二轮已回写（W1-0 契约先行交付：18 条契约 + SKILL.md 三承诺 + 反漂移门禁 23 测试 + 两 workflow 接线；**门禁自身缺口由负向 N2 首跑抓出并按《预防措施》#13 加固**；PRD §6 (18)）**；✅ **第十三轮已回写（W1-1 E2 规则解析层：四层链 + 第一个匹配生效 + 系统层兜底 + 安全豁免硬编码；契约 C4/C5 两处**先行**修订并经门禁咬合验证（关键词 `file order` 缺口由门禁变红抓出）；`tests/test_rule_engine.py` 26 项 + 16 subTest，**5 条受控突变全部被抓住**；零门禁变更；PRD §6 (19) + §6 (18) off-by-one 更正）**
> **下一步**: ① ~~关闭 D5~~ ✅ → ② ~~W0-1 / W0-2 / W0-3a–f / W0-4~~ ✅ → ③ ~~W0-5 全量回归~~ ✅ → ④ ~~对外文档同步 + release notes~~ ✅ → ⑤ ~~提交 + 推送分支 + tag `v4.5.20`~~ ✅ → ⑥ ~~改 lock 的 anyio 4.13.0→4.14.2 + 改 flaky 判据 + P2-1 环境隔离 + P2-2 告警日志（本地全绿：`9345 passed` / `SUITE3_EXIT=0`）~~ ✅ → ⑦ ~~提交 + 推分支 `v4.5.20-patch` + 开 PR `#10`~~ ✅ → ⑧ ~~裁定 P2-3（Moka 401）/ 是否打新 tag（→ 不切 tag）~~ ✅（第八轮） → ⑨ ~~本轮改动（auto 链顺序 + 推理预算契约 + size gate 拆文件）追加到 PR #10 新 commit；确认 `security` job 转绿~~ ✅（CI run `35839990737` 全绿 + Release E2E `35839999897` 全绿，含 6 项首次执行门禁） → ⑩ ~~裁定版本位归属~~ ✅（第九轮：**不得升 MINOR，维持 `4.5`**）+ ~~修复 3 个 mypy 存量错误~~ ✅ → ⑪ ~~收口门禁可信度（去 `--no-site-packages`）+ 全量时序断言改造~~ ✅（第十轮）+ ~~W1 准入复核~~ ✅（维持准予进入） → ⑫ ~~裁定并实施四项遗留（`st.mermaid` / P2-4 / black 成因 / tag）~~ ✅（第十一轮：前三项已实施，tag 缓办；含一次显式授权的门禁变更 = `ruff format --check` 接入两个 workflow） → ⑬ ~~进入 W1（W1-0 契约先行）~~ ✅（第十二轮：18 条契约 C1–C18 + SKILL.md 三承诺章节 + 反漂移门禁 `check_skill_contract.py` 23 测试 + `test.yml::lint` / `release-e2e.yml::final-gate` 双接线；**门禁自身缺口由负向 N2 首跑抓出并按《预防措施》#13 加固为行内取值**） → ⑭ ~~W1-1（E2 规则解析层：四层链 + 第一个匹配生效 + 系统层兜底）~~ ✅（第十三轮：`rule_engine.py` + `system_rules.json` + package-data；契约 C4/C5 **先行**修订并经门禁变红咬合验证；26 项 + 16 subTest；**5 条受控突变全部被抓住**；零门禁变更；第十四轮补充修复 `persist_dir` 跨秒恢复缺陷并完成 `8010 passed` 全量复核） → ⑮ **W1-2（`devsquad rules check <path>` 解释命令，C8）** —— `resolve()` 的**首个生产调用方**，须照 `scripts/cli.py` 的 `register_*_subparser` + `handlers` 别名表惯例新增 `scripts/cli_rules.py`；遗留待裁定：无（tag 属发布动作，已裁定"W1 后一并打"）
> **本轮实质结论**: **W0 以 MINOR = 4.5.20 完成发布，但发布门禁未闭合**。tag `v4.5.20` 已推送、功能类 CI 全绿；**唯一红项 = `requirements.lock` 钉住的 `anyio==4.13.0`（2 条 CVE）**，它同时打红了 `test.yml` 的 `security` job 与 `Release` 工作流的 `Run pip-audit hard check`，**并连带使 Docker/Compose、Helm lint、Helm chart、bandit、dispatcher 体积、测试计划一致性 6 项门禁从未执行**（发布态未知，不得记作通过）。另有 P1 = `test_write_performance` 的单次绝对时序断言在共享 runner 上间歇红（本机 90.4 ms vs CI 3823.6 ms，约 42×）。**处置已按用户裁决执行并本地验证通过（PRD §6 (1)–(9)）**：anyio → 4.14.2；flaky 判据改为"相对本环境同操作基线"（保留 1000 ms 地板，受控实验证明仍能抓真实退化）；P2-1 环境隔离；P2-2 推理模型预算耗尽 → 仅告警日志、**不改返回行为**。**CI 等价全量段 `9345 passed, 2 skipped` / `SUITE3_EXIT=0`**；CI 侧 P0 闭环仍待 PR 验证。**同时订正第六轮一处未取证的解释**：`tests/external` 2 项并非"外网依赖"，实为推理模型预算耗尽与 `.env` 的 `MOKA_*` 入链（见 (6)/(7)）。
> **第八轮补充结论**：修好 anyio 后 `Release` 后半段门禁首次真正执行 → 暴露 **P0-2（dispatcher size gate 被本批改动打红，run `35696422202`）**，按用户裁决**抽取重构转绿（基线未改、行为未改）**；**P2-2 改判**：由"只加告警"改为"**告警 + 按模型抬高预算（`DEFAULT_LLM_MAX_TOKENS_REASONING=16384`）+ 空串不再当成功（`raise RuntimeError`）**"（**原裁决基于不完整证据**，见 §6 (11)）；**auto 链顺序**由 `API_BACKEND_ORDER` 单点定为 **Moka → OpenAI(DeepSeek) → Anthropic → Mock**（如实记录"最初问 Moka不再用、复问后明确 Moka 优先"的反转）；**Moka 中转 Key 实测 401 → 用户裁定「顺序不动、.env 也不动」，登记为已知并接受的行为**。**本批含 BREAKING，不切 tag；版本位归属未定。CI 等价全量段 `9368 passed, 2 skipped` / `SUITE_RUN3_EXIT=0`（= 9345 + 23）。**
> **第九轮补充结论**：① **版本纪律裁定** —— 用户明令「**不得升 MINOR**，未经批准不得实施 MINOR 升级」，故第八轮遗留的「版本位归属未定」**就此关闭**：`VERSION` 维持 `4.5.20`（`4.5` 线内是否切 PATCH tag 属发布动作，**仍待裁定**）；② **3 个 mypy 存量错误根因修复**（无 `type: ignore`、未放宽检查、未改门禁），同时**暴露 mypy 门禁 `--no-site-packages` 假绿盲区** —— 同一工作树下去掉该 flag 即复现 3 个错误、带上则 0 错误，**"门禁绿"曾被当作"类型正确"的结论不成立**（与 P0-2 同类，见 PRD §6 (13)）；③ **W1 准入判定 = 准予进入**：CI `35839990737` 全绿、Release E2E `35839999897` 3 job 全绿且 **step 10–16 首次真正执行**（含 P0-2 的 size gate 红转绿），**无测试失败**；顾虑 5 条均为非阻断（mypy flag 盲区 / `st.mermaid` 死分支 / black 存量红：`scripts/ tests/` 域 `450/234`、整仓 `.` 为 `455/246`，且 CI `lint` 不含 black / P2-4 未归因 / PATCH tag 未切，见 PRD §6 (14)）；④ **独立复核（fresh-eyes 子代理）推翻了我方一处自我夸大**：mypy 修复的**第一版**顺手删掉了 `st.mermaid` 分支原有的 `except RuntimeError` 回退，这已不是"纯类型修复"而是潜在行为回归，且注释「keeps the branch working if a future Streamlit adds it」言过其实 —— **已把 `except RuntimeError` 补回**，并以三态探针证明修复前后行为一致（PRD §6 (13)）。**该子代理同时复现了核心主张（`--no-site-packages` 掩盖 3 个错误、双配置 exit 0、ruff B009、Streamlit 1.57.0 无 `st.mermaid`、版本未升），未发现造假。** ⑤ **最终提交双轨复核**：纠正落盘后重跑，`test.yml` run `35939211514` 与 `release-e2e.yml` run `35939924215` **均全绿**，后者 step 10–16 逐条 success —— W1 准入的取证基线是"最终提交双轨全绿"，而非仅某一轮。

> **第九轮后补（2026-09-24）**：上述复核之后，同分支的纯文档提交 `9f586e9` 触发 `test.yml` run `35940420527` **`test (3.12)` 红**（`test_dependency_hallucination_checker.py::T4_Performance::test_01_scan_1000_lines_under_200ms`，`406.46 not less than 200.0`）。**该红不构成本轮 W1 判定的反例**：`git diff --name-only 69a271d 9f586e9 | grep -c '\.py$'` = **0**，且同一份代码在 run `35939211514` 为 success —— 属**环境性 flaky**。本机同断言体重复 15 次：`min=43.06 median=44.22 max=51.74 ms`（0/15 超预算），CI 406.5 ms ≈ **9.2×**，与 P1-1 同一失效模式。**该反模式在测试集中至少还有 9 处**；且**已用受控实验证伪"P1-1 比值法可直接套用"**（coverage 插桩下比值漂移 +24%：`plain 1.541x → coverage 1.913x`）。**本轮未改动任何测试判据**（改判据属门禁变更，须用户裁定），已在 PRD §6 (15) 登记为已知 flaky 并给出 A/B/C/D 四方案待裁定。**W1 结论维持：准予进入。**
> **第十轮补充结论**：① **mypy 门禁假绿盲区已收口** —— 获用户授权后 `--no-site-packages` 从 3 处 mypy 步骤全部移除，并附**负向探针**证明门禁仍真的在检查（`scripts/_mypy_probe_tmp.py` → 报 `return-value` 错误，随即删除）；同时把 `[tool.mypy] python_version` 由 3.10 抬到 3.12（原因 = flag 去掉后 mypy 会解析已安装 stub，而 `numpy>=2.5` 的 `.pyi` 含 PEP 695 `type` 语句；**已注明 CI 用的是 numpy 2.4.6，CI 本身不会因此红**，"本地能复现"未被写成"CI 曾失败"）；② **全量单次绝对时序断言改造**（`(15)` 登记的"至少 9 处"经全量搜索实为 **36 处 / 20 文件**）改为**环境等比天花板** `perf_ceiling_ms(budget_ms)`，控制载荷与 DevSquad 模块无关；**首版"同代码路径控制"被受控实验证伪**（注入 13.7× 慢化时，同路径控制同步膨胀 → 原判据抓得住、新判据反而放过），已回退；③ **W1 准入维持"准予进入"**（真实 CI `35971001606` + Release E2E `35971053128` 在 `cac2ba7` 上全绿）；④ 提交 `cac2ba7` 已推送至 `v4.5.20-patch`。
> **第十一轮补充结论**：① **四项非阻断遗留全部裁定并实施**（`st.mermaid` 死分支**删除**并更正三处错误陈述 / **P2-4 机制级归因闭合**（性质 = 测试未隔离环境输入，非产品回归）并修复 / **black 全仓红成因定案** = "从未有任何格式化器被强制过"，非 black 缺陷、非版本漂移 / **tag 缓办至 W1 后**）；② **唯一一次门禁变更已获显式授权**：采纳 `ruff format` 为**唯一**格式化门禁，并**一次性全仓 reformat**（`435 files reformatted`），`ruff format --check .` 接入 `test.yml::lint` 与 `release-e2e.yml::final-gate`，**负向验证** `GREEN_EXIT=0 / RED_EXIT=1 / RESTORED_EXIT=0`；③ **reformat 打红了两个阻断级门禁，均已修复**：size gate（7 文件超基线，用门禁自带 `--write-baseline` 刷新，**门禁逻辑/阈值/比较方式未改**；顺带发现旧基线早已 stale）与 `check_test_quality.py`（红点是 **docstring 散文**里的 `@lru_cache` 被行级正则命中 + 豁免是**位置型窗口**，reformat 插入 1 空行即出窗 —— **处置 = 改 fixture 而非放宽门禁**）；④ **诚实登记**：size 基线刷新抬高了 7 个上限、2 个文件首次越过 800（`dependency_hallucination_checker.py` 740→805、`worker.py` 791→837）；MINOR 373→371 的两条差额已逐条定量；⑤ **主动作废自己本轮的一处错误结论**（reformat "引入 8 条无效 noqa 警告" —— 受控对照证明为**存量**，此前未现身只因 ruff 缓存），并记录一次**修复失败**（红点先从 269 行移到 271 行）。**tag 仍未打，`VERSION` 仍为 `4.5.20`。**
> **第十三轮补充结论**：**W1-1 交付 E2 规则解析层**（`rule_engine.py` + `system_rules.json` + `package-data` + `tests/test_rule_engine.py` 26 项/16 subTest），实现契约 **C4–C7**：四层链（CLI → 项目 → 用户 → 随包系统层）、**第一个匹配生效（无 merge / 无 override，层内按文件顺序）**、**系统层永远存在**（末条 `**` catch-all，否则 `resolve()` 报错）、**安全豁免硬编码**（19 条路径模式，先于所有可配置层执行）。① **契约先行被验证是承重墙**：按 E7 先改契约 C4/C5 再写代码，改完首跑契约门禁即**变红** `FAIL: [rule.first_match_wins] keyword 'file order' is missing...` —— 我原措辞 "rule order in the file" 不符门禁字面量，**处置 = 改文档，不改关键词表**（这是 W1-0 门禁真正在工作的证据，而非装饰）。② **契约两处修订方向是收紧语义定义**（"层存在但不可解析 = `RuleConfigError`，仅*缺失*静默"、"右锚定 + 前置 `**/` 可匹配零级目录"），不是因为代码而放宽。③ **glob 方言扩展由探针定量决定**：实测 `PurePosixPath("cli.py").match("**/*.py")` = `False`（`fnmatch` 同），无扩展则随包系统层漏判所有顶层 Python 文件 —— 真脚枪，故写入契约。④ **5 条受控突变 + 1 条对照**（`/tmp` 整包副本，仓库零改动）：fail-open → `exit=1 4 failed`；merge → `6 failed`；丢兜底 → `1 failed`；缺层报错 → `9 failed`；砍方言 → `7 failed`；对照 `26 passed` —— **全部被抓住**（《预防措施》#13）。⑤ **诚实边界**：本模块**尚无生产调用方**，首个调用方为 **W1-2 的 `rules check`**；在 W1-2 落地前**不得记作"已接入管线"**（F4 教训）。⑥ **零门禁变更**（契约门禁已在 W1-0 接线），**tag 仍未打，`VERSION` 仍为 `4.5.20`，未升 MINOR**。
> **第十四轮补充结论（2026-09-24）**：W1-1 收口时发现并修复一个与规则解析无关的存量 P0：`Scratchpad(persist_dir=...)` 未提供显式 id 时，秒级时间戳同时作为 JSONL 文件名，跨秒销毁重建会静默恢复 0 条，违反 US-1.6 / AC-1.6.3。修复源码为按目录续用最新合法 `scratchpad-YYYYmmdd-HHMMSS.jsonl` 会话，并新增跨秒确定性回归测试；未修改原有 `test_e2e_6`。受控实验修复前 auto-id 跨秒 `LOST (before=2, after=0)`，修复后 `RECOVERED`。修复后全量 CI 等价 pytest：`8010 passed, 14 skipped, 6 deselected, 1 warning in 448.73s`，coverage `79.70%`；外层命令 exit 1 仅由预期负向 sandbox probe 访问禁路径触发，pytest 本身 exit 0。结论：W1-1 收口完成，未新增门禁，tag 继续按既有裁定延后至 W1 后。

> **触发 7-Role 复核的条件**: D7 确认自证盲区 / W3-0 论证建议实施 / F4 改走废弃路径