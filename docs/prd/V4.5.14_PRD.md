# DevSquad V4.5.14 PRD — Real-Listener Trace Collection Archived + Honest-Status / Log-Noise Fixes

> **日期**: 2026-09-02　**基线**: v4.5.13 (`156cac9`)　**类型**: PATCH（无新用户功能、无 Breaking）

## 1. 立项依据

V4.5.13 backlog "TRAE 监听方在线时跑采集脚本归档真实 PASS" 待关闭；联调过程中发现 `_safe_read_json` 对**不存在**的响应文件也打 `JSON decode failed` 告警，导致 `trace_3` 误诊。

## 2. 交付内容

### 2.1 真实监听方 trace 归档 5/5（docs/e2e_evidence/V4.5.12_trae_ide_real/collected/）

- trace_1 success round-trip：48.4s
- trace_2 subagent mapping：architect 34.3s / security 18.1s
- trace_3 fuse threshold：2×timeout → fuse skip → 第三次 `BackendUnavailable`（success）
- trace_4 cross-version isolation：success，`v1_marker_untouched=true`
- trace_5 resource bound：`fail_closed`（预期 PASS）

监听方 = 宿主 LLM agent 会话（读 marker → 按 prompt_file 执行 → `HostLLMBridgeV2.write_response` 写回响应信封）。每条 trace 均含 `result.json`、`response_*.raw`（原始字节捕获）与 `v2_snapshot/`。

### 2.2 trace_3 诚实状态修复

`create_backend("host")` 在 `TRAE_ENV` 被剥离的环境会抛 `BackendUnavailable`，此前导致未捕获 traceback 崩溃（违反诚实状态契约）。现转为诚实 `fail` 状态并附错误信息；fuse 探测同时透传 `--wait-seconds`（原先固定 600s）。

### 2.3 `_safe_read_json` 缺席文件日志伪影修复

v2 `wait_for_response` 对**不存在**的响应文件也会执行 3 次重试并打出 `JSON decode failed after 3 retries` 警告。修复：缺席文件 = 正常轮询，立即返回 `None` 不告警；仅"存在但不可解析"才重试 + 告警。

### 2.4 关键更正：V4.5.13 "真实监听方写非 JSON 响应" 误诊

两个 60s 受控探针（项目默认 v2 目录、/var/folders 临时目录各一）均无任何响应文件出现 → **不存在常驻后台 marker 监听方**。此前记录的"真实监听方写非 JSON 响应"系上述日志伪影被误读为证据。

## 3. 验收标准

- AC-1: `docs/e2e_evidence/V4.5.12_trae_ide_real/collected/trace_1..5/result.json` 全部存在
- AC-2: `collect_trae_traces.trace_3` 的 `BackendUnavailable` 转 `fail` 状态（不抛 traceback）
- AC-3: `HostLLMBridgeV2._safe_read_json` 缺席文件立即返回 `None`，不告警；坏坏文件仍 warn
- AC-4: 单元测试 `TestSafeReadJson::test_absent_file_returns_none_without_warning` + `test_existing_bad_json_retries_then_warns` 通过

## 4. 门禁

- ruff 0
- v2 bridge + trace collector 43 passed
- 一致性 54 passed
- 全量回归 ≥9539 passed / 0 failed

## 5. 发布

- commit `5ce4b6d`
- tag `v4.5.14`
- push origin main --tags