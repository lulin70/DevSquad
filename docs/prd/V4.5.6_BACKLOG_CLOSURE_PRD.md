# DevSQuad V4.5.6 PRD — Backlog Closure

> **版本**: V4.5.6 (Backlog Closure: 技术债清零)
> **类型**: PATCH 升级 (无新功能，纯闭环)
> **日期**: 2026-08-25
> **基线**: V4.5.6 (commit afabeb0, Anti-Ghost 18/18, 9048 passed, tag v4.5.6)
> **目标**: 让 V4.5.6 → V4.5.6 实现"零技术债"状态，为 V4.6.x MINOR 新功能腾出纯净基线

---

## 1. 背景与问题

### 1.1 V4.5.6 收官盘点（实测 2026-08-25）

| 检查 | V4.5.6 状态 | V4.5.6 目标 |
|------|-------------|-------------|
| Anti-Ghost gate | 18/18 PASS ✅ | 保持 18/18 |
| ruff | 0 errors on V4.5.6 new modules ✅ | 0 errors 全 repo |
| check_version_consistency | 47/47 PASS ✅ | 47/47 |
| test_pyramid | 74.5% / 15.3% / 5.3% healthy ✅ | healthy |
| check_doc_consistency | PASS ✅ | PASS |
| **check_test_quality** | **66 MAJOR findings** ❌ | **0 MAJOR** |
| **test_no_secrets_in_repo** | **1 failed (2 placeholder secrets)** ❌ | **PASS** |
| **test_real_llm_\*** | **失败 (MOKA_API_KEY 无效)** ❌ | **PASS (mock fallback)** |
| **`_call_counter → _er` 全统一** | **74 文件 / 422 行未替换** ❌ | **0 未替换** |
| **G6 SKILL.md 诚实标注** | **partial** ❌ | **complete** |

### 1.2 backlog 来源溯源

| # | backlog | 来源版本 | 严重度 | 影响 |
|---|---------|----------|-------|------|
| B1 | 66 MAJOR findings in `check_test_quality.py` | V4.5.2 遗留 | P1 | 阻塞真测试质量评估 |
| B2 | `_call_counter → _call_counter_er` 全统一 (74 文件) | V4.5.3 lesson #4 留债 | P2 | 命名不一致增加维护成本 |
| B3 | `test_no_secrets_in_repo` 失败 (2 placeholder secrets) | V4.5.4 引入 | P1 | CI 失败，security gate 红 |
| B4 | `test_real_llm_*` 失败 (MOKA_API_KEY 无效) | V4.5.2 引入 | P2 | CI 失败但 env-failed 已知 |
| B5 | G6 SKILL.md 诚实标注 (partial) | V4.5.6 留债 | P2 | weiransoft v2.8.4 对齐 5/6 → 6/6 |

### 1.3 业务需求

V4.5.6 是首次多目标复合迭代（5 Wave 同推），达到历史新高 7-Role 共识 9.1/10。但**留下 5 项技术债**，下次 MINOR 迭代（V4.6.x 真新增功能）会因这些债而失速。

**V4.5.6 目标**: 把这 5 项债全部闭环，让 V4.5.6 → V4.6.x 的跳跃是"纯增量"而非"带债增量"。

---

## 2. 范围 (Scope)

### 2.1 Wave 1 — `_call_counter` → `_call_counter_er` 全统一

**目标**: V4.5.3 lesson #4 100% 闭环（V4.5.6 完成 50%，剩 50%）

**范围**:
- 74 文件含 `_call_counter` (非 `_er` 后缀)
- 422 行需替换
- 涉及模块: `dispatcher / file_bundler / report_formatter / viewpoint_registry / quality_calibration_gate / error_budget_tracker / approval_gate / dispatch_hooks / protocols / skill_provider_builtin / gitlab_connector / dependency_hallucination_checker / scratchpad_history_store / dora_metrics_collector / host_llm_bridge / models_dispatch / role_specific_mock_backend / gap_analyzer / perf_baseline / order_chain_detector / agent_identity / task_scale_gate / connector_framework / backend_config / ...`

**替换规则**:
- `_call_counter: dict` → `_call_counter_er: dict` (V4.5.3 命名约定)
- `get_call_counter()` → `get_call_counter_er()` (API 一致)
- `_inc_call_counter()` → `_inc_call_counter_er()`
- 所有引用方同步更新
- 测试文件中 `from module import get_call_counter` → `get_call_counter_er`

**测试**:
- 单元 (10 tests):
  - `test_call_counter_er_renamed_in_dispatcher`
  - `test_call_counter_er_renamed_in_report_formatter`
  - `test_call_counter_er_renamed_in_viewpoint_registry`
  - `test_get_call_counter_er_api_uniform`
  - `test_check_module_activation_references_er_version`
  - `test_no_legacy_call_counter_references` (反查)
  - `test_call_counter_er_thread_safe`
  - `test_call_counter_er_reset_for_test_isolation`
  - `test_call_counter_er_visible_in_dispatch_report`
  - `test_call_counter_er_increment_consistent`

**风险**:
- 模块间耦合度高，需全量回归
- check_module_activation.py 反向引用新名

### 2.2 Wave 2 — 66 MAJOR findings 修复

**目标**: check_test_quality.py 报的所有 MAJOR findings 清零

**已识别范围**:
- `tests/test_api_security.py`: 13 findings (anti-magic-number / anti-loose-assert)
- `tests/test_rate_limit.py`: 16 findings
- `tests/test_api_server_v362.py`: 33 findings
- `tests/test_workflow_engine_*.py`: 4 findings

**修复模式**:
- `assertTrue(x == y)` → `assertEqual(x, y)` / `assert x == y`
- 魔法值 (0.95 / 100ms / 16x16 等) → 提取到模块顶部常量
- anti-loose-assert: 改用 `assertEqual` / `assertIn` / `assertIs` 精确断言

**测试**:
- 回归: 修复后 4 文件测试仍全绿
- `check_test_quality.py` 报 0 MAJOR

### 2.3 Wave 3 — `test_no_secrets_in_repo` 修复

**目标**: 删除/替换 2 个 placeholder secrets

**已识别问题**:
- `tests/integration/test_v454_v453_modules_integration.py:355` — `sk-fake-test-redaction-key-001`
- `tests/integration/test_v454_v453_modules_integration.py:361` — 同上

**修复模式**:
- 改为 `_get_test_api_key("openai")` fixture 返回 `sk-test-fake-key-for-unit-test-only` (高熵但非真实模式)
- 或用 `monkeypatch.setenv("OPENAI_API_KEY", "sk-test-fake")` 替换
- placeholder 模式不应触发 secrets scanner 真实告警

**测试**:
- `test_no_secrets_in_repo.py::TestT1CodeNoPlaintextKey::test_full_repo_scan_no_secrets` PASS
- placeholder 替换后 `tests/integration/test_v454_v453_modules_integration.py` 仍全绿

### 2.4 Wave 4 — `test_real_llm_*` 失败修复

**目标**: MOKA_API_KEY 无效时跳过 (env-failed 标记) 而非真失败

**修复模式**:
- 添加 `pytest.mark.skipif(not os.getenv("MOKA_API_KEY"), reason="MOKA_API_KEY not set")`
- 或 `MOKA_API_KEY="invalid"` 时使用 mock fallback (best-effort)
- 与 V4.5.3 LLM 测试模式对齐 (V4.5.2 引入的 `responses` 库 mock 模式)

**测试**:
- `test_real_llm.py` + `test_real_llm_smoke.py`: MOKA_API_KEY 未设 → skip (exit 0)
- MOKA_API_KEY="valid" 时真跑

### 2.5 Wave 5 — G6 SKILL.md 诚实标注 (complete)

**目标**: weiransoft v2.8.4 G6 partial → complete

**范围**:
- `SKILL.md` 顶部新增章节 "⚠️ Honest Disclosure" (V4.5.6 partial 完成)
- 标注:
  - 提示词层 AI 由宿主 LLM 完成 (TRAE IDE 内)
  - 脚本层确定性工具 (Python CLI/API)
  - 无网络时降级 TFIDF/Hashing (V4.5.0 fallback)
  - 真实 LLM 路径仅在 DevSQuad IDE 内嵌 dispatch 时生效
  - TRAE IDE 外调用 dispatcher → B-path timeout 600s → 降级 A/C (mock)

**测试**:
- `check_version_consistency.py` G6 explicit check: PASS
- SKILL.md 包含关键词 "Honest Disclosure" + 5 项降级说明

---

## 3. 非范围 (Out of Scope)

V4.5.6 是 **纯闭环 PATCH**，不新增任何功能：

- ❌ 不新增模块（保持 197 个核心模块）
- ❌ 不新增 Anti-Ghost counter（保持 18/18）
- ❌ 不引入新依赖（保持 855 源文件）
- ❌ 不修改 dispatcher 主流程
- ❌ 不修改 V4.5.6 已交付的 4 个新模块 (host_llm_bridge_v2 / dispatcher_transaction / dispatcher_intent_mapper / dispatcher_loop_controller)

---

## 4. 验收标准 (Acceptance)

| 项 | 验收方式 | 目标 |
|----|----------|------|
| Anti-Ghost | `check_module_activation.py` | 18/18 PASS |
| ruff | `ruff check scripts/ tests/` | 0 errors |
| py_compile | `python -m py_compile` 4 new modules | OK |
| test_pyramid | `check_test_pyramid.py` | 74.5% / 15.3% / 5.3% healthy |
| check_test_quality | `check_test_quality.py` | **0 MAJOR findings** (was 66) |
| test_no_secrets | `pytest tests/security/test_no_secrets_in_repo.py` | **PASS** (was 1 failed) |
| test_real_llm | `pytest tests/test_real_llm*.py` | **PASS or skip** (was 3 failed) |
| _er 命名统一 | `grep -rn "_call_counter[^_]" scripts/ tests/` | **0 matches** (was 74 files) |
| check_version_consistency | `check_version_consistency.py` | 47/47 PASS |
| check_doc_consistency | `check_doc_consistency.py` | PASS |
| G6 SKILL.md | `check_version_consistency.py` G6 check | complete (was partial) |
| 7-Role 共识 | 共识表 | ≥ 9.0/10 (维持 V4.5.6 高位) |
| 测试总数 | `pytest --collect-only -q` | 9048+ (V4.5.6 baseline + 修复回归) |

---

## 5. 工作量与时间表

| Wave | 工作量 | 风险 | 优先级 |
|------|--------|------|--------|
| W1 _er 全统一 | ~3h | 中 (74 文件回归) | P0 (闭环 lesson) |
| W2 66 MAJOR | ~2h | 低 (机械替换) | P1 |
| W3 no_secrets | ~30min | 极低 (placeholder 替换) | P1 |
| W4 real_llm | ~1h | 低 (skip 标记) | P2 |
| W5 SKILL.md G6 | ~30min | 极低 (文档增补) | P2 |
| **合计** | **~7h** | **中** | — |

---

## 6. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| W1 大规模 rename 引入新 bug | 高 | 增量替换 (每次 10 文件 + 测试) + 强 ruff/check_module_activation 反查 |
| W2 修复引入测试覆盖缺失 | 中 | 修复 magic number 时同时提取常量到 _CONSTANTS，并加 unit test |
| W3 placeholder 替换破坏原意 | 低 | fixture 提供语义化 fake key |
| W4 skip 标记掩盖真 bug | 中 | 显式 `pytest.mark.skipif(reason=...)` 而非 `@pytest.skip` |
| W5 文档覆盖不全 | 低 | G6 check 必须包含 5 项降级说明 |

---

## 7. V4.5.6 → V4.6.x 跳跃

V4.5.6 闭环后，V4.6.x MINOR 可大胆做：

- 新增 AI 类型 (RealLLM 路径稳定化)
- 新增 persistent Transaction (SQLite WAL)
- IntentWorkflow YAML 支持
- `_call_counter_er` Prometheus exporter
- V4.7.x 进入"零技术债 + 真功能"双轨

---

## 8. 7-Role 共识目标

| Role | 目标分 | 关注点 |
|------|--------|--------|
| Architect | 9.0 | 5 Wave 同推 + W1 rename 风险 |
| PM | 9.0 | 时间表 ~7h 合理 |
| Security | 9.5 | W3 placeholder 修复 + G6 honest |
| Tester | 9.0 | W2 magic number → constants + 回归 |
| Coder | 9.0 | W1 rename 模式 + W2 机械替换 |
| DevOps | 9.0 | W4 skip env-failed 清晰 |
| **平均** | **≥ 9.1** | 维持 V4.5.6 历史新高 |