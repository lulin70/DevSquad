# DevSquad V4.5.13 P2 共识 + P3 设计（合并评审）

> **日期**: 2026-08-31　**对象**: `docs/prd/V4.5.13_PRD.md`　**状态**: ✅ 通过

## P2 三贤者复审

| 角色 | 评分 | 要点 |
|---|---|---|
| 架构 | 8.7 | 采集脚本与既有 HostLLMBridgeV2 协议零耦合（只走公共 create_request/wait_for_response）；远程 fs 检测封装在 store 内部，无跨模块扩散 |
| 测试 | 8.6 | 三态 status 枚举防伪造 PASS；monkeypatch statvfs 使 errno/远程检测可确定性测试；/metrics 冒烟走 TestClient 无需起服务器 |
| 安全 | 8.6 | 采集脚本只写项目内目录与 v2 bridge 目录（0700/0600 权限沿用）；不新增网络面；errno 白名单窄（ESTALE/EREMOTE/EBADRPC），EAGAIN 超时不误报 |

**结论**：≥8.5 门禁通过，PRD 落定。

## P3 决策 ↔ 实现位置映射

| # | 决策 | 实现位置 |
|---|---|---|
| D1 | 技能缓存单一来源 | 环境操作（已执行）：删 `~/.trae-cn/skills/devsquad`、`~/.trae/skills/devsquad`、`DevSquad/.trae/skills/devsquad` + `.bak`；`scripts/check_version_consistency.py` 缓存层适配 |
| D2 | 一键采集脚本 | `scripts/collect_trae_traces.py`（TRACES 注册表 5 场景 → create_request/wait_for_response → 归档 `collected/trace_N/`） |
| D3 | 跨主机信号自动埋点 | `file_risk_store.py`：`_looks_like_remote_fs(path)`（statvfs ST_REMOTE）+ `_REMOTE_ERRNOS = {ESTALE, EREMOTE, EBADRPC}`，在 `__init__` 与 `_open_lock` flock 异常路径调用 |
| D4 | /metrics 暴露 | `prometheus_metrics.py` `DevSquadMetrics.__init__` 注册 4 指标 + `record_risk_store_stats(stats)`；`api/routes/metrics.py` docstring 补序列名 |

## 测试矩阵

| 文件 | 数量 |
|---|---|
| `tests/unit/test_v4513_trace_collector.py` | ≥6 |
| `tests/unit/test_v4513_cross_host_auto.py` | ≥5 |
| `tests/integration/test_v4513_prometheus_v4512.py` | ≥4 |
