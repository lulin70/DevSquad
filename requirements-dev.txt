# Auto-generated from pyproject.toml [dev] optional-dependencies
# For development: pip install -r requirements-dev.txt
# =======================================================

# Testing
pytest>=7.0
pytest-asyncio>=0.21
pytest-cov>=3.0
httpx2>=2.5.0
fakeredis>=2.30
redis>=5.0
hypothesis>=6.0

# Code Quality
black>=23.0
flake8>=6.0
mypy>=1.0
ruff>=0.4.0

# Visualization & QA (synced from pyproject.toml [dev])
streamlit>=1.28.0
Pillow>=10.0.0

# LLM Backends (optional — install only what you need)
# openai>=1.0           # OpenAI backend (pip install -e .[openai])
# anthropic>=0.18        # Anthropic backend (pip install -e .[anthropic])

# MCP Server (optional — for Cursor integration)
# mcp>=0.9              # MCP protocol (pip install -e .[mcp])
