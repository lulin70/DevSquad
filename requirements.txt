# DevSquad V3.6.0-Prod Dependencies
# ===================================

# Core Dependencies (Required)
pyyaml>=6.0

# Production Server Stack (V3.6.0 NEW)
fastapi>=0.100.0              # REST API server framework
uvicorn[standard]>=0.23.0     # ASGI HTTP server
streamlit>=1.28.0             # Web dashboard
pydantic>=2.0                 # Data validation (for API)
slack-sdk>=3.21.0             # Alert notifications (optional)

# Optional LLM Backends (install only what you need)
# openai>=1.0                  # OpenAI backend (pip install devsquad[openai])
# anthropic>=0.18              # Anthropic backend (pip install devsquad[anthropic])

# Optional Memory Layer
# carrymem>=0.8               # CarryMem portable AI memory (pip install devsquad[carrymem])

# Optional MCP Server
# mcp>=0.9                    # MCP protocol (pip install devsquad[mcp])

# Development & Testing (pip install devsquad[dev])
# pytest>=7.0
# pytest-asyncio>=0.21
# black>=23.0
# flake8>=6.0
# mypy>=1.0

# Install options:
#   pip install -e .                    # Core + Production features
#   pip install -e .[openai]           # + OpenAI backend
#   pip install -e .[anthropic]        # + Anthropic backend
#   pip install -e .[all]              # All optional dependencies
#   pip install -e .[dev]              # Development tools
